# 09 Implementation Readiness Audit

> **Version:** Pass 4.8D sync + 4.8D-cache metadata hook + 4.8E sync + 4.8F final readiness reassessment — 2026-05-15 (Pass 4.8F: full audit re-run on disk; 0 active open questions confirmed; 0 active findings confirmed; Gate A upgraded 🟡→🟢; Gate C upgraded 🟡→🟢; Gate B confirmed 🟢; verdict upgraded NEEDS_CROSS_SPEC_CLEANUP_FIRST→READY_FOR_IMPLEMENTATION_PLAN; docs/11_CLI_MVP_IMPLEMENTATION_PLAN.md creation now authorized; no code/schema/runtime/OpenClaw/provider touched)
> **Status:** All MVP schema batches (A/B/C/D) created and accepted. CLI MVP implementation complete (Phases 0–12). Harness implemented with 28 fixtures (27 passed, 1 approved-skipped — Fixture 13 `safety_hard_protection`, covered by SHP-1 unit test). 651/651 tests pass. Gate B: `SATISFIED WITH 1 APPROVED SKIP(S)`. Gate D: intentionally out of MVP scope.
> **Scope:** Sections 1–10 written. Audit complete for current spec state.
> **Phase 13 note:** Current Phase 13 status supersedes historical Pass 4.8F implementation-prohibition wording found in §1, §9, and §10.3. See Phase 13 status-sync note at end of this document.

---

## 1. Executive Summary

- Selector Orchestration deterministic specification is complete at **Pass 3.2.6** (including Pass 3.2.6.3 metadata cleanup).
- All four **Section 21 budget-aware open questions** (Q1–Q4) are resolved.
- The spec covers: purpose, inputs/outputs, 8 selector types, 12-step deterministic ladder, conflict resolution, per-selector signal rules, injection-suspect integration, budget-aware hints, expensive_optional threshold, over_budget_protected warn-only policy, and budgetHint survival through Conflict Resolution.
- **All cross-spec open questions are now resolved/reference** after Pass 4.8E — **0 active open questions** remain. All 27 inventory rows are resolved/reference (5-Q1, 5-Q2, 5-Q3, 5-Q4, 5-Q5, 5-Q6, 5-Q7, 5-Q8, 9-Q1, 9-Q2, 9-Q3, 9-Q4, 9-Q5, 12-Q1, 12-Q2, 12-Q3, 12-Q4, 12-Q5, 15-Q1, 15-Q2, 15-Q3, 15-Q4, 15-Q5, 18-Q1, 18-Q2, 18-Q3, 18-Q4). Safe-defer items (5-Q3/F-26, 5-Q4, 5-Q5, 5-Q7/F-28, 12-Q5, 15-Q5, F-30, F-31) were closed as explicit non-MVP notes in Pass 4.8E.
- **Implementation planning is now authorized.** This audit covers the open-question inventory, Part B1 field/enum/object-shape findings, Part B2 behavior/data-flow/trace-flow findings, Part B3 MVP/non-MVP boundary findings, readiness gates (Section 6), and final verdict (Section 9). The verdict is **READY_FOR_IMPLEMENTATION_PLAN** (upgraded in Pass 4.8F) — `docs/11_CLI_MVP_IMPLEMENTATION_PLAN.md` creation is now authorized. Actual code implementation remains prohibited until the CLI MVP Implementation Plan is written and approved.

---

## 2. Source Documents Read

| File | Why it matters | Read status in this audit sequence |
|---|---|---|
| `docs/06_SELECTOR_ORCHESTRATION_SPEC.md` | Authoritative Selector Orchestration spec — all decisions, ladder logic, open questions | ✅ Read in this pass (Pass 3.2.6 + 4.5A + 4.5B + 4.5B.1 + 4.7A + 4.7A.1 + 4.7B + 4.7B.1 + 4.7B.2 + 4.7C + 4.8B + 4.8B.1 + 4.8B.2 + 4.8C + 4.8C.1 + 4.8C.2 + 4.8D source sync + 4.8D.1 cleanup + 4.8D.2 accounting cleanup + 4.8E-2A safe-defer notes + 4.8E-2B safe-defer notes, 2833 lines) |
| `PROJECT_MASTER_PLAN.md` | Project identity, MVP scope, legal/safety rules, module overview, evaluation plan | ✅ Read in this pass |
| `docs/04_PORTABLE_CORE_ARCHITECTURE.md` | Module boundaries, registry-as-source rule, selector read-only contract | ✅ Read in this pass (Pass 4.1B.1, 745 lines) |
| `docs/05_COMPONENT_REGISTRY_SPEC.md` | Registry field definitions (`retainPolicy`, `budgetPriority`, `tokensApprox`, `formatTag`, evidenceRequired atom governance, etc.) used by selectors | ✅ Read in this pass (Pass 11 + 4.8E-2B F-30 cross-reference, 643 lines) |
| `docs/00_NORTH_STAR.md` | Mission: smaller context only when safe; fail-open; explainable; portable | Not read in this pass; referenced from master plan only |

---

## 3. Completed Deterministic Spec Surface

The following areas are fully specced in `docs/06_SELECTOR_ORCHESTRATION_SPEC.md` (Pass 3.2.6):

- **Purpose and scope** — what Selector Orchestration does and does not do; fail-open rule stated at module level (§1)
- **9 normalized inputs** — request signals, registry indexes, runtime capabilities, history summary, budget state, user constraints, selector policy, prompt family, active component IDs; strictness classes A/B defined (§2)
- **6 outputs + candidateSetSummary trace record** — SelectionDecision records, selectorTrace, selectorSummary, planningWarnings, unknownReferences, budgetHintSummary (derived); **`candidateSetSummary`** emitted in `registryPhase` before fan-out: `candidateSetPolicy` (MVP: `all_non_quarantined`), `candidateSetSize` (gap-check denominator), `quarantinedExcluded`; `conflictSummary.totalComponents` tied to `candidateSetSummary.candidateSetSize`; 5 candidateSetSummary harness checks; unsupported policy halts with `unsupported_candidate_set_policy` (5-Q8/F-29 resolved, Pass 4.8D) (§3.1)
- **SelectionDecision object** — 10 fields, all action values, all path values, omission constraint (only Path A / Path B), evidence rules (§4)
- **8 selector types** — scaffold, skill, tool, history, memory, policy, output_format, runtime_capability; primary ownership by `type`; **singular canonical `type` per component** — multi-role components resolved at registry authoring time by type choice or separate registry entries; no inter-selector handoff in MVP; type-ownership violation if multiple primary selectors independently evaluate same component (9-Q3 resolved, Pass 4.8D); unrecognized-type handling (§7)
- **12-step deterministic ladder** — Priority 0 pre-check, Steps 1–12; Path A (§7 safe-omit), Path B (§8 default-omit); fail-open step 11; ladder fallback step 12; 7 unconditional invariants; **Step 3 always emits `path: safety_override`** for all four hard-protection conditions — former `required_match` exception removed; secondary `requiredWhen` match preserved as trace atoms in `evidence[]`/`constraintsApplied[]` only; `path: required_match` from Step 3 is a harness failure (9-Q2 resolved, Pass 4.8D) (§8)
- **Conflict Resolution** — **12 conflict cases** (Case 12: `history_malformed_fail_open` — history-malformed fail-open include vs omit, specialized Case 1 sub-case; hard-protection preempts; `history_malformed_conflict` per-conflict warning; `history_malformed_conflict_occurred` global per-run warning deduplicated — 12-Q4/F-22 resolved, Pass 4.8D); 8-level priority table; `resolutionRule` strict enum (**14 canonical values** including `history_malformed_fail_open`, §11.3.1a); **Case 5 `losingDecisions` semantics fixed** — only true losers; `multiple_include_merged` cannot hide hard protection (Pass 4.8D.1); `noConflictComponentIds` lightweight ID list for no-conflict accounting; full `conflictResolutionTrace` entries for actual conflicts only; 8 required trace fields + 4 optional gate-conversion fields; **16 conflict harness checks** (Pass 4.8B added 2; Pass 4.8C added 2; Pass 4.8D added 5 for history-malformed conflict cases) (§11)
- **Per-selector deterministic signal rules** — all 8 selectors; positive/negative signals; fail-open triggers; warning codes; trace atoms; not-allowed list (§14)
- **Injection-suspect integration** — post-ladder injection gate; `warn_and_continue` vs `fail_open_all`; `halt_planning` reserved-future (recognized, not implemented); orchestrator-owned exactly-once global warning per run (F-18 resolved, Pass 4.7C); **scoped high-risk output_format bias under `warn_and_continue`** (`riskLevel: critical/high` output_format omit → `include/fail_open` + `injection_suspect_policy_override` — 18-Q1 resolved, Pass 4.8C); **familyConfidence escalation rule** (if `injectionSuspect: true` and `familyConfidence < failOpenThreshold`, effective policy escalates to `fail_open_all`; uses existing `failOpenThreshold`; `policyFallbackReasons: string[]` replaces singular `policyFallbackReason` — 18-Q3 resolved, Pass 4.8C); per-selector effect summary; 6 per-decision trace fields + 3 global-planning-trace fallback fields (`policyFallbackReasons[]`); 17 harness checks (Pass 4.8C added 4) (§17)
- **Budget-aware selector hints** — informational only; 5 budgetHint values; per-selector logic; global rule; Section 23 threshold cross-reference (§20)
- **expensive_optional threshold policy** — 500 token static MVP threshold; 3 classification rules; charsApprox fallback; eligibility criteria (retainPolicy: optional required); 10 harness checks (§23)
- **over_budget_protected warn-only policy** — action/path unchanged; warn + risk flag; no halt in MVP; 9 trace fields; 7 harness checks (§25)
- **budgetHint survival through Conflict Resolution** — resolved decisions canonical; global priority order (§27.5); 3-step merge process; supporting fields travel with hint source; optional derived budgetHintSummary; 7 harness checks (§27)

---

## 4. Open Question Inventory

> [!NOTE]
> This section preserves the open-question inventory for historical traceability. All 27 rows are now **resolved/reference** after Pass 4.8E. Historical issue text is retained as-is for audit continuity. No new decisions are being made in this cleanup pass.

### Section 5 — Pass 1 Open Questions (8 questions)

| ID | Short title | Exact issue (concise) | Affected module | Blocks schema? | Blocks harness? | Blocks impl? | Risk | Recommended disposition |
|---|---|---|---|---|---|---|---|---|
| 5-Q1 | ~~`requestSignals` finalization~~ | **Resolved/reference Pass 4.7B (F-25).** Decision: `requestSignals.injectionSuspect` is frozen as `boolean` for MVP. Selectors consume this boolean only; they never inspect raw user text or pattern codes. The Request Router is the sole detection owner. Malformed `injectionSuspect` (non-boolean when `requestSignals` is otherwise valid) → treat as `false`, emit `injection_suspect_malformed` planning warning. Future richer metadata (e.g., `injectionEvidenceCodes?: string[]`) must use additive optional fields only — must not replace the boolean type. A struct replacement requires an explicit future cross-spec pass. Canonical wording in Orchestration spec §2.1. | — | — | — | — | — | Resolved — no action needed. |
| 5-Q2 | ~~`selectorTrace` placement~~ | **Resolved/reference Pass 4.5A.** Decision: `selectorTrace` is embedded in `trace.json` under a `selectorPhase` key (`trace.json.selectorPhase.selectorTrace`). There is no separate `selector-trace.json` in MVP. The full `trace.json` is a keyed phase object. Future externalization reserved as a non-MVP extension only. Architecture §7.8 and Orchestration spec §3.2 updated. F-15 resolved. | CLI output, Evaluation Harness | — | — | — | — | Resolved — no action needed. |
| ~~5-Q3~~ | ~~`runtimeCapabilities` versioning~~ | **Resolved/reference Pass 4.8E-2A (5-Q3 / F-26 — safe-defer).** Decision: `capabilityTimestamp` and `capabilityVersion` are future optional fields for adapter-supplied capability snapshots. In MVP, `runtimeCapabilities` is operator-authored per run; automated drift detection is out of scope; `runtimeLabel` is the only MVP runtime identifier. No warning is emitted for absent version/timestamp fields in MVP. See docs/06 §2.5 non-MVP boundary note. | — | — | — | — | — | Resolved — no action needed. |
| ~~5-Q4~~ | ~~`reference_unknown` field clarity~~ | **Resolved/reference Pass 4.8E-2A (5-Q4 — safe-defer).** Decision: for `action: reference_unknown`, `componentId` carries the caller-supplied unknown string — not a registry-validated ID. This dual-use is intentional in MVP. A separate `unknownId` field is deferred to schema v1.1. `SelectionDecision` shape unchanged. Harness must not compare `reference_unknown.componentId` against `componentsById`. See docs/06 §4 `reference_unknown` row and key rules harness note. | — | — | — | — | — | Resolved — no action needed. |
| ~~5-Q5~~ | ~~`userConstraints` trust boundary~~ | **Resolved/reference Pass 4.8E-2A (5-Q5 — safe-defer).** Decision: MVP `userConstraints` are operator-supplied CLI input, treated as high-trust after schema conformance. Session-derived lower-trust constraints are future adapter work requiring a trust-level mechanism, per-constraint validation, and a refined safety-override spec. Existing safety-override behavior unchanged. Do not add `constraintTrustLevel` or session-constraint validation in MVP. See docs/06 §2.8 non-MVP boundary note. | — | — | — | — | — | Resolved — no action needed. |
| 5-Q6 | ~~`selectorSummary` narrative generation~~ | **Resolved/reference Pass 4.5B.** Decision: `selectorSummary.narrative` is generated by the Selector Orchestration output phase using a fixed deterministic template string populated from count fields. Model-generated narrative is future-only and must not be implemented in MVP. Template and rules defined in Orchestration spec §3.6. | — | — | — | — | — | Resolved — no action needed. |
| ~~5-Q7~~ | ~~Dedicated `unavailable` action~~ | **Resolved/reference Pass 4.8E-2A (5-Q7 / F-28 — safe-defer).** Decision: MVP keeps `action: defer` + `path: runtime_unavailable`. A dedicated `action: unavailable` is future-only and must not be added to the action enum. `path` is required on every `deferredComponents[]` entry; harness distinguishes defer subtypes by filtering on `path`. A future `deferSubtype` field may be considered only if harness or schema requirements prove it necessary. See docs/06 §4 `runtime_unavailable` row + key rules harness note; docs/04 §7.7 `deferredComponents[]` path-required note. | — | — | — | — | — | Resolved — no action needed. |
| 5-Q8 | ~~Scoped evaluation policy~~ | **Resolved/reference Pass 4.8D (F-29).** Decision: `candidateSetPolicy` is an internal orchestrator constant, MVP value `all_non_quarantined`. `candidateSetSummary` emitted in `registryPhase` before fan-out (`candidateSetPolicy`, `candidateSetSize`, `quarantinedExcluded`). Gap-check denominator is `candidateSetSize`. `conflictSummary.totalComponents` tied to `candidateSetSummary.candidateSetSize`. Unsupported policy halts with `unsupported_candidate_set_policy`. `reference_unknown` records tracked separately, not subtracted from denominator (4.8D.2). See Orchestration spec §3.1. | — | — | — | — | — | Resolved — no action needed. |

---

### Section 9 — Pass 2 Open Questions (5 questions)

| ID | Short title | Exact issue (concise) | Affected module | Blocks schema? | Blocks harness? | Blocks impl? | Risk | Recommended disposition |
|---|---|---|---|---|---|---|---|---|
| 9-Q1 | ~~`quarantine` action placement~~ | **Resolved/reference Pass 4.7A (F-17).** Decision: `quarantine` is registry-phase state only in MVP. Not a valid `SelectionDecision.action`. Quarantined components never reach selectors. Step 1 of ladder updated to boundary-violation detection: if a quarantined ID appears in fan-out despite the registry guarantee, the orchestrator produces `action: include`, `path: quarantine_boundary_violation`, `confidence: low` and emits `unexpected_quarantine_reference` planning error. Quarantine may be revisited for future non-MVP streaming-registry scenarios. See Orchestration spec §4 F-17 note and §8 Step 1. | — | — | — | — | — | Resolved — no action needed. |
| 9-Q2 | ~~Hard-include path for `mandatory` vs `safety_critical`~~ | **Resolved/reference Pass 4.8D.** Decision: Step 3 always emits `path: safety_override` for all four hard-protection conditions (`retainPolicy: safety_critical`, `retainPolicy: mandatory`, `omissionPolicy: never`, `riskLevel: critical`). Former exception allowing `required_match` when `retainPolicy: mandatory` coincides with a `requiredWhen` match has been removed. Secondary `requiredWhen` match preserved as trace atoms in `SelectionDecision.evidence[]` and `constraintsApplied[]` using existing syntax — no new atom grammar or new fields. `path: required_match` from Step 3 is a harness failure. See Orchestration spec §8 Step 3. | — | — | — | — | — | Resolved — no action needed. |
| 9-Q3 | ~~Multi-role component type ownership~~ | **Resolved/reference Pass 4.8D.** Decision: each component has exactly one canonical `type`; the primary selector for that `type` is the sole evaluator. Registry authors resolve multi-role intent at authoring time by choosing the most safety-relevant `type` or by splitting into separate registry entries. No inter-selector handoff mechanism exists or is planned for MVP. A component appearing in independent evaluations from multiple primary selectors is a type-ownership violation. See Orchestration spec §7 fan-out rule. | — | — | — | — | — | Resolved — no action needed. |
| 9-Q4 | ~~Path A null evidence warning code~~ | **Resolved/reference Pass 4.8B.** Decision: a distinct per-decision warning code `path_a_null_evidence` is emitted in `SelectionDecision.warnings` whenever Path A authorizes an omit with `evidenceRequired: null`. Enables the Evaluation Harness to distinguish intentional null-evidence omissions from accidental ones. Not a global planning warning. Registry semantics unchanged: `evidenceRequired: null` means no additional evidence expression beyond standard Path A gates is required. See Orchestration spec §8 Step 7 for canonical wording. | Harness, trace schema | — | — | — | — | Resolved — no action needed. |
| 9-Q5 | ~~Path B confidence convention~~ | **Resolved/reference Pass 4.8B.** Decision: `confidence: high` for all Path B (`default_action_omit`) omissions. Rationale: Path B is fully deterministic — all conditions must hold exactly before the step fires. Confidence describes selector-rule certainty, not registry-author judgment quality. A `medium` or `low` Path B omit is a planning error. See Orchestration spec §8 Step 8 for canonical wording. | SelectionDecision schema | — | — | — | — | Resolved — no action needed. |

---

### Section 12 — Pass 3.1 Open Questions (5 questions; all resolved/reference)

| ID | Short title | Exact issue (concise) | Affected module | Blocks schema? | Blocks harness? | Blocks impl? | Risk | Recommended disposition |
|---|---|---|---|---|---|---|---|---|
| 12-Q1 | ~~No-op conflict trace granularity~~ | **Resolved/reference Pass 4.8B.** Decision: no-conflict components (single unambiguous decision) are recorded only in `noConflictComponentIds` — a lightweight `string[]` ID list. Full `conflictResolutionTrace` entries are emitted only for components with actual conflicts. `conflictSummary.noConflict` count still covers all no-conflict components, preserving accounting completeness. Harness checks `noConflictComponentIds.length + conflictResolutionTrace.length` equals total evaluated candidate count. See Orchestration spec §11.3.2 for canonical wording. | Conflict trace schema | — | — | — | — | Resolved — no action needed. |
| 12-Q2 | ~~`resolutionRule` as enum vs string~~ | **Resolved/reference Pass 4.8B.** Decision: `resolutionRule` is a **strict enum** of coded values in MVP. Free strings are not allowed. Canonical enum values defined in Orchestration spec §11.3.1a. Any unrecognized value is a harness failure. Future additions require an explicit cross-spec decision pass. Existing conflict-case and priority-rule names from §11.4–11.5 form the first version. | Conflict trace schema, Harness | — | — | — | — | Resolved — no action needed. |
| 12-Q3 | ~~Priority 3 vs `alwaysInclude` for unavailable tools~~ | **Resolved in-spec (Pass 3.1)** — Priority 0 pre-check; `runtime_unavailable` beats `alwaysInclude` for `type: tool`. | — | — | — | — | — | Resolved — no action needed. |
| 12-Q4 | ~~History malformed + conflict case~~ | **Resolved/reference Pass 4.8D (F-22).** Decision: this is primarily a traceability gap, not a safety-correctness gap — Case 1 (include beats omit) already resolves correctly because `path: fail_open` is not `path: not_evaluated`. New Case 12 defined with `resolutionRule: history_malformed_fail_open` — fires when winning include has history-malformed provenance and no Priority 0–4 rule applied. Hard protection preempts: if a hard-protection include is also present, `resolutionRule` is `safety_hard_protection` and history-malformed context goes to `warningsEmitted` only. Warning codes: `history_malformed_conflict` (per-conflict trace); `history_malformed_conflict_occurred` (global per-run, deduplicated). `losingDecisions` contains only true losers. 5 new harness checks. See Orchestration spec §11.5 Case 12 and §11.3.1a. | — | — | — | — | — | Resolved — no action needed. |
| ~~12-Q5~~ | ~~`conflictSummary` breakdown by priority~~ | **Resolved/reference Pass 4.8E-2B (12-Q5 — safe-defer).** Decision: a `byPriority` breakdown field on `conflictSummary` is deferred to future/v2 evaluation tooling. Per-priority conflict counts are derivable post-hoc from `conflictResolutionTrace` by grouping entries on `resolutionRule`. The `narrative` field is freeform text for `summary.md` only; harness must not parse or assert on `narrative` content. See docs/06 §11.3.4 non-MVP boundary note. | — | — | — | — | — | Resolved — no action needed. |

---

### Section 15 — Pass 3.2.1 Open Questions (5 questions; all resolved/reference)

| ID | Short title | Exact issue (concise) | Affected module | Blocks schema? | Blocks harness? | Blocks impl? | Risk | Recommended disposition |
|---|---|---|---|---|---|---|---|---|
| 15-Q1 | ~~`evidenceRequired` atom governance~~ | **Resolved Pass 4.3B** — Registry spec §7 Atom Governance subsection added. Registry spec is sole canonical owner of `evidenceRequired` atoms. Selector-phase trace atoms (§14) are not `evidenceRequired` atoms and cannot authorize omission. Active MVP atoms: `promptFamily=<v>`, `riskLevel=<v>`, `explicitUserConstraint=false`. Unknown atoms disable Path A + emit warning. | — | — | — | — | — | Resolved — no action needed unless new atoms are proposed; new atoms require a cross-spec pass. |
| 15-Q2 | ~~Active ID validation ownership~~ | **Resolved/reference Pass 4.8B.** Decision: validation of `activeSkillIds`, `activeToolIds`, `activeMemoryIds` against `componentsById` is owned by the **core boundary / orchestrator**, not by individual selectors. Validation runs once before selector fan-out. Unknown active IDs → `active_id_unknown` planning warnings (one per ID) + `unknownActiveIds[]` list. Selectors receive pre-validated lists and treat them as trusted. Unknown active IDs do not produce `reference_unknown` decisions — the two warning classes are distinct. See Orchestration spec §15 Q2 for canonical wording. | Core boundary, selector input | — | — | — | — | Resolved — no action needed. |
| 15-Q3 | ~~History `lane` field absence~~ | **Resolved in-spec (Pass 3.2.1.1)** — `history_lane_missing` warning + fail-open. Confirm `lane` field name against registry spec before implementation. | — | — | — | — | — | Resolved/reference — historical implementation note retained; no active open question remains in this audit. |
| 15-Q4 | ~~`formatTag` field name confirmation~~ | **Resolved Pass 4.3A** — Registry spec §3 Optional MVP Fields now defines `formatTag` (string | null, default null) for `type: output_format` components. Absent/null means no `outputFormatHint` match can be authorized; selector must fail open. | — | — | — | — | — | Resolved — no action needed unless future output_format fields are added. |
| ~~15-Q5~~ | ~~`capabilityInventoryComplete: false` omission gate~~ | **Resolved/reference Pass 4.8E-2B (15-Q5 — safe-defer, harness-sensitive).** Decision: selector behavior is already correct — Path A and Path B omission for `runtime_capability` components are gated on `capabilityInventoryComplete: true`. Fail-open inclusion with the appropriate warning is correct behavior when inventory is incomplete (`false` or absent). Harness fixtures for `runtime_capability` omission must explicitly set `capabilityInventoryComplete: true`; with `false`/missing inventory, fail-open inclusion is the expected and correct result. See docs/06 §14.8 Evaluation Harness boundary note. | — | — | — | — | — | Resolved — no action needed. |

---

### Section 18 — Pass 3.2.2 Open Questions (4 questions; 4 resolved/reference rows — 18-Q1, 18-Q2, 18-Q3, 18-Q4)

| ID | Short title | Exact issue (concise) | Affected module | Blocks schema? | Blocks harness? | Blocks impl? | Risk | Recommended disposition |
|---|---|---|---|---|---|---|---|---|
| 18-Q1 | ~~`output_format` injection bias~~ | **Resolved/reference Pass 4.8C.** Decision: scoped output_format bias under `warn_and_continue`. If `injectionSuspect: true`, effective policy is `warn_and_continue`, and component is `type: output_format` with `riskLevel: critical` or `riskLevel: high`, the injection gate converts ladder `omit` to `include / fail_open` and emits `injection_suspect_policy_override`. `riskLevel: low` or `riskLevel: medium` output_format components are not biased. Canonical rule in Orchestration spec §17.3.1; per-selector summary §17.5; harness check §17.7. | — | — | — | — | — | Resolved — no action needed. |
| 18-Q2 | ~~Third injection policy `halt_planning`~~ | **Resolved/reference Pass 4.7B (F-24).** Decision: `halt_planning` is recognized as a known future/non-MVP value. If an operator config supplies `halt_planning` in MVP, the orchestrator emits `policy_value_not_implemented` planning warning and applies `warn_and_continue` as the safe fallback — this is distinct from `injection_action_unknown` (used for typos/unknown values). Halt planning semantics are not defined in MVP and must not be implemented. Canonical wording in Orchestration spec §2.9 `injectionSuspectAction` table. | — | — | — | — | — | Resolved — no action needed. |
| 18-Q3 | ~~`injectionSuspect` + low `familyConfidence` interaction~~ | **Resolved/reference Pass 4.8C.** Decision: if `requestSignals.injectionSuspect === true` and `requestSignals.familyConfidence < selectorPolicy.failOpenThreshold` (strict less-than), the effective injection policy escalates to `fail_open_all` regardless of the configured `injectionSuspectAction`. Uses existing `failOpenThreshold` (no new field). `family_confidence_fail_open_escalation` warning emitted once into `planningWarnings`. `policyFallbackReasons: string[]` records all resolution steps in order (replaces former singular `policyFallbackReason`). Policy chain table and `halt_planning` chaining defined in Orchestration spec §17.3.4. | — | — | — | — | — | Resolved — no action needed. |
| 18-Q4 | ~~Global injection warning deduplication~~ | **Resolved/reference Pass 4.7C (F-18).** Decision: the **orchestrator** emits exactly one global injection warning per planning run. Selectors and per-decision injection gates must not emit the global warning codes (`injection_suspect_warn_and_continue`, `injection_suspect_fail_open_all`) independently. Orchestrator maintains conceptual `globalInjectionWarningSent` boolean; emission occurs once after policy normalization. Warning code determined by effective policy. Fallback cases emit one policy-level warning + one effective-policy global warning. Per-decision codes (`injection_suspect_omit_allowed`, `injection_suspect_policy_override`) are distinct and not subject to this rule. Three new harness checks added to Orchestration spec §17.7. See §17.6 for canonical wording. | — | — | — | — | — | Resolved — no action needed. |

---

## 5. Cross-Spec Consistency Findings

> [!NOTE]
> This section preserves the cross-spec consistency finding inventory for historical traceability. All findings are now **resolved/reference** or already resolved in prior passes. Historical descriptions are retained as-is for audit continuity. No source specs are edited in this cleanup pass.

### 5.1 Field / Enum / Object-Shape Findings

| Finding ID | Severity | Files / Sections | Description | Why it matters | Recommended next action |
|---|---|---|---|---|---|
| F-01 | ~~**blocker**~~ ✅ **resolved** | `04` §7.3 vs `06` §4 | `SelectionDecision` shape mismatch. Architecture defines 5 fields: `{componentId, action, confidence, evidence[], riskFlag}`. Orchestration spec defines a 10-field object with different names: `selectorName`, `reason`, `path`, `constraintsApplied`, `warnings`, `traceRefs` added; `riskFlag` absent. | Schema cannot be written consistently until one definition is canonical. | **Resolved Pass 4.2A:** Architecture §7.3 now references `docs/06_SELECTOR_ORCHESTRATION_SPEC.md` §4 as canonical. Stale 5-field shape and `riskFlag` removed from Architecture. `summarize` marked future-only. |
| F-02 | ~~**blocker**~~ ✅ **resolved** | `04` §7.4 vs `06` §11 | `ConflictResolver` output shape mismatch. Architecture says output is "a single resolved `SelectionDecision` with `resolvedBy` and `conflictTrace[]`". Orchestration spec defines `ResolvedSelectionDecision` (distinct object) with fields `componentId`, `finalAction`, `finalPath`, `inputDecisionIds`, `losingDecisions`, `resolutionRule`, `warningsEmitted`, `resolvedAt`, plus budget hint fields. `resolvedBy` and `conflictTrace[]` are not present in Orchestration spec. | Trace schema, prompt plan schema, and Budgeter input all depend on this shape. | **Resolved Pass 4.2A:** Architecture §7.4 now references `docs/06_SELECTOR_ORCHESTRATION_SPEC.md` §11/§27 `ResolvedSelectionDecision` as canonical. Stale `resolvedBy`/`conflictTrace[]` fields superseded. |
| F-03 | ~~**blocker**~~ ✅ **resolved** | `04` §7.6 vs `05` §5 | `HistoryTurn.retainPolicy` enum mismatch. Architecture defines `retainPolicy` on `HistoryTurn` as `"always" \| "unless_budget_critical" \| "prefer_drop"`. Registry spec defines the component-level `retainPolicy` enum as `optional \| durable \| mandatory \| safety_critical`. These are different enums on different objects sharing the same field name — causing collision risk in implementation and schema. | Implementors will confuse the two; a shared schema file cannot hold both without namespace separation. | **Resolved Pass 4.2B:** Architecture §7.6 renamed `HistoryTurn.retainPolicy` → `HistoryTurn.turnRetentionPolicy` (enum: `always \| unless_budget_critical \| prefer_drop`). Disambiguation note added. Component-level `retainPolicy` in Registry spec unchanged. |
| F-04 | ~~**major**~~ ✅ **resolved** | `05` §7 vs `06` §14.4, §15-Q1 | `evidenceRequired` atom set governance unresolved. Registry spec defines the recognized atoms. Orchestration spec §14 selectors use selector-specific trace atoms (e.g., `active_skill_id_match`, `output_format_hint_match`). Governance decision needed. | Unregistered atoms disable Path A silently. Harness cannot verify atom coverage. | **Resolved Pass 4.3B:** Registry spec §7 now has an Atom Governance subsection establishing Registry spec as the sole canonical owner of all `evidenceRequired` atoms. Selector-phase trace atoms (§14) are not `evidenceRequired` atoms — they are positive-include-only signals that are logically incompatible with omission authorization. The active MVP atom set is `promptFamily=<v>`, `riskLevel=<v>`, `explicitUserConstraint=false`. Unknown/unlisted atoms disable Path A + emit validation warning. 15-Q1 resolved. |
| F-05 | ~~**major**~~ ✅ **resolved** | `05` §3 vs `06` §14.7, §15-Q4 | `formatTag` field missing from Registry spec. Orchestration spec §14.7 output_format selector references `formatTag` as a required registry field for format-hint matching. This field does not appear in Registry spec §3 (required or optional fields table). Open question 15-Q4 flags this. | Selector cannot implement format-hint matching without the field. Schema will fail to validate. | **Resolved Pass 4.3A:** `formatTag` (string \| null, default null) added to Registry spec §3 Optional MVP Fields. Meaningful only for `type: output_format` components. If absent or null, selector fails open rather than omitting based on this field. |
| F-06 | ~~**major**~~ ✅ **resolved** | `05` §7 vs `06` §2.7, §20 | `budgetCritical` atom ownership ambiguity. Registry spec §7 listed `budgetCritical=true` as a recognized `evidenceRequired` atom, implying selectors could use budget pressure to authorize Path A omission. Orchestration spec §20 states: "Budget pressure cannot authorize omit" and "Selectors are budget-aware but not budget-enforcing." | Registry authors could use this atom to make selectors enforce budget limits — a hidden budget-enforcement path contradicting the Orchestration spec. | **Resolved Pass 4.3B (Option B):** `budgetCritical=true` removed from the active MVP `evidenceRequired` atom set. Moved to a "Not Supported in MVP" table in Registry spec §7 with rationale: using it would violate the Orchestration spec §20 invariant. `budgetState.budgetCritical` remains available to selectors as context for informational budget hints (§20.4 `budgetCriticalObserved`), but it cannot authorize omission. Future scoped path requires a formal cross-spec pass. |
| F-07 | ~~**major**~~ ✅ **resolved** | `05` §7 vs `06` §2.1 | `requestFamily` vs `promptFamily` atom name mismatch. Registry spec §7 defines the atom as `requestFamily=<value>`. Orchestration spec §2 consistently uses `promptFamily` as the field name. | At evaluation time, the selector reads `promptFamily` from its input but evaluates an atom named `requestFamily` — a clear name collision that will cause atom evaluation to silently fail. | **Resolved Pass 4.3A:** Registry spec §7 atom table now uses canonical `promptFamily=<value>`. All `evidenceRequired` examples updated. Compatibility note added: `requestFamily` is not canonical for MVP; registry authors must use `promptFamily=<value>`. |
| F-08 | ~~**major**~~ ✅ **resolved** | `04` §7.8 vs `06` §4 | `TraceEntry.confidence` type mismatch. Architecture §7.8 defined `"confidence": 0.95` (float) in the trace entry shape. Orchestration spec §4 defines `confidence` on `SelectionDecision` as `high \| medium \| low` (string enum). These are two different types for a field with the same name in overlapping objects. | Trace schema cannot be written consistently without a canonical definition. | **Resolved Pass 4.3C (Option C/A):** `SelectionDecision.confidence` remains canonicalized by `docs/06_SELECTOR_ORCHESTRATION_SPEC.md` §4 as `high \| medium \| low` (string enum) — this is the only canonical selector-decision confidence field. Architecture §7.8 TraceEntry conceptual example updated: `confidence: 0.95` replaced with `confidence: "high"`. A canonical note was added: numeric confidence scores (e.g., `requestSignals.familyConfidence: float` from the Request Router) already use distinct field names and must not reuse the `confidence` field on selector decisions or their trace entries. No new fields invented. Orchestration spec §4 unchanged — it was already correct. |
| F-09 | ~~**minor**~~ ✅ **resolved** | `04` §7.4 vs `06` §11.4 | Conflict resolver priority table mismatch. Architecture §7.4 listed a stale 8-level priority table. Orchestration spec §11.4 defines Priority 0 (runtime correctness pre-check) plus Priorities 1–7 with canonical names. | Implementors reading Architecture would implement the wrong priority table. | **Resolved Pass 4.4:** Architecture §7.4 stale 8-level table replaced with a reference to Orchestration spec §11.4 as canonical. Priority 0 (runtime correctness pre-check for `type: tool`) now reflected. |
| F-10 | ~~**minor**~~ ✅ **already resolved** | `04` §7.3 vs `06` §7 | Selector action enum mismatch. `summarize` was listed as an active MVP action in Architecture §7.3; `quarantine` and `reference_unknown` were absent. | A future model-assisted selector returning `summarize` would be unhandled by the Orchestration spec. | **Already resolved prior to Pass 4.4 and synced after Pass 4.7A:** Architecture §7.3 (Pass 4.2A) already states that `summarize` is **future-only** and not part of the MVP deterministic action set. Current MVP selector actions are `include`, `omit`, `defer`, and `reference_unknown`. `quarantine` is registry-phase state only in MVP after F-17 (Pass 4.7A) — it is not a `SelectionDecision.action`. No further edit needed. |
| F-11 | ~~**note**~~ ✅ **resolved** | `04` §7.1 vs `05` §7 | `evidenceRequired` null semantics divergence. Architecture §7.1 said `null` means "no omission allowed". Registry spec §7 says `null` means no additional evidence expression required; `safeToOmitWhen` match alone is sufficient for Path A. The Architecture wording was stricter and contradicted the Registry spec's authoritative definition. | A developer reading Architecture would incorrectly block all Path A omissions for null-evidence components. | **Resolved Pass 4.4:** Architecture §7.1 `evidenceRequired` description updated to match Registry spec §7 semantics. Clarified: `null` does not by itself authorize or block omission; it means no additional evidence expression beyond the standard Path A gates is required. |

> **Note:** Lower-risk findings (e.g., `HistoryTurn` field naming in Architecture §7.6 vs lane enum in Architecture §7.6 itself, `selectorPolicy` field name in Architecture vs Orchestration spec) are deferred to a future cross-spec cleanup pass.

### 5.2 Behavior / Data-Flow / Trace-Flow Findings

| Finding ID | Severity | Files / Sections | Description | Why it matters | Recommended next action |
|---|---|---|---|---|---|
| F-12 | ~~**blocker**~~ ✅ **resolved** | `04` §7.5 vs `06` §27 | Budgeter input type mismatch. Architecture §7.5 defines Budgeter input as "List of resolved `SelectionDecision` objects". Orchestration spec §27 defines `ResolvedSelectionDecision` (distinct type) as the canonical Budgeter input, carrying `budgetHint`, `thresholdCrossed`, and other budget fields. The Architecture module has the wrong input type — the Budgeter will not receive budget hint information if wired per the Architecture doc. | Budget hint survival through Conflict Resolution (a core Pass 3.2.6 decision) is silently lost if the Budgeter receives pre-resolution `SelectionDecision` objects. | **Resolved Pass 4.2C:** Architecture §7.5 now specifies Budgeter input as canonical `ResolvedSelectionDecision` records (per Orchestration spec §11/§27). Budget-related fields (`budgetHint`, `budgetWarningCodes`, `tokensApproxObserved`, etc.) documented. Resolved decisions remain the authoritative Budgeter input. |
| F-13 | ~~**blocker**~~ ✅ **resolved** | `04` §7.7 vs `06` §3 | `defer` action has no representation in the prompt plan output shape. Architecture §7.7 prompt plan output has `selectedComponents` and `omittedComponents` only. Orchestration spec produces `action: defer` decisions (runtime_unavailable, default_defer) that are neither selected nor omitted. There is no `deferredComponents` field or equivalent in the Architecture plan shape. | A deferred tool (runtime_unavailable) silently disappears from the plan. Operators cannot distinguish "deferred" from "omitted", breaking audit and trace requirements. | **Resolved Pass 4.2C:** Architecture §7.7 prompt plan output now includes `deferredComponents[]`. Selected/omitted/deferred are distinct lists. `defer/runtime_unavailable` and `default_defer` are distinguishable by `path`. Deferred components must not be counted as budget savings. |
| F-14 | ~~**major**~~ ✅ **resolved** | `04` §7.5 vs `06` §20–27 | `budgetHint` mechanism partially absent from Architecture Budgeter. *(Partially mitigated Pass 4.2C: Architecture §7.5 now accepts `budgetHint`, `budgetWarningCodes`, `tokensApproxObserved`, and related fields from `ResolvedSelectionDecision`.)* | **Resolved Pass 4.6:** Architecture §7.5 expanded with: (1) `budgetHint` interpretation table for all 5 MVP canonical values (`protected` → never trim; `over_budget_protected` → never trim, emit `budget_infeasible_protected_component` risk flag, set `budgetOverflow: true`; `candidate_optional` → trim-eligible in priority order; `expensive_optional` → trim-eligible, preferred over `candidate_optional` at equal priority; `unknown_cost` → use conservative default, emit `budget_cost_unknown` warning) plus absent/null fallback; (2) **no unsafe omission invariant** — budget pressure is not a valid omission path; (3) **no mutation invariant** — Budgeter reads resolved decisions as read-only, emits separate `BudgetReport`; (4) expanded conceptual `BudgetReport` shape with `trimTrace`, `conservativeEstimatesUsed`, `riskFlags`; (5) **budget infeasibility behavior** — retain protected/mandatory, set `budgetOverflow: true`, emit risk flag, do not halt, aligns with §25 warn-only; (6) **PPG handoff** — PPG receives unmodified `ResolvedSelectionDecision[]` + `BudgetReport`; PPG surfaces overflow/risk flags; PPG must not reinterpret `budgetHint` or override Budgeter; `budgetHintSummary` computed only after `BudgetReport` received; (7) evaluation expectations expanded. Canonical `budgetHint` values remain owned by `docs/06_SELECTOR_ORCHESTRATION_SPEC.md` §20/§27. No new values invented. `budgetHintSummary` remains PPG final-assembly output only; Budgeter does not consume it in MVP. |
| F-15 | ~~**major**~~ ✅ **resolved** | `04` §7.8 vs `06` §3.2, §5-Q2 | `selectorTrace` structure conflict. Architecture §7.8 described `trace.json` as a flat array of `TraceEntry` objects. Orchestration spec §3.2 embedded `selectorTrace` under a `selectorPhase` key inside `trace.json`, producing a nested structure. A flat-array schema and a keyed-nested schema are mutually exclusive. Open question 5-Q2 flagged this as unresolved. | `trace.schema.json` cannot be written until the structure is decided. Both the Evaluation Harness and any trace viewer will break if the structure is wrong. | **Resolved Pass 4.5A:** MVP trace container chosen as a **keyed phase object** (Option D). Architecture §7.8 updated: `trace.json` is no longer described as a flat array; it is a keyed phase object with `run`, `requestPhase`, `registryPhase`, `selectorPhase` (containing `selectorTrace[]`), `conflictPhase`, `budgetPhase`, `planPhase`, and `warnings` keys. Orchestration spec §3.2 updated: `selectorTrace` embedded under `trace.json.selectorPhase.selectorTrace`; no separate `selector-trace.json` in MVP; future externalization reserved as non-MVP extension. 5-Q2 resolved/reference. |
| F-16 | ~~**major**~~ ✅ **resolved** | `04` §6 vs `06` §3.1 | Gap-check / `not_evaluated` synthetic decision step absent from Architecture data flow. Architecture §6 showed Selectors → Conflict Resolver with no intermediate gap-check. Orchestration spec §3.1 requires the orchestrator to detect unevaluated candidates after fan-out and inject synthetic `action: include / path: not_evaluated / confidence: low` decisions before handing off to the Conflict Resolver. | Without this step an implementation built from the Architecture diagram would silently drop unevaluated components. | **Resolved Pass 4.4:** Orchestrator Gap-Check step added explicitly to Architecture §6 data flow between Section Selectors and Conflict Resolver, with description of its behavior. Request Router annotation also updated to reflect injection-suspect signal emission. |
| F-17 | ~~**major**~~ ✅ **resolved** | `05` §10 vs `06` §4, §8 step 1, 9-Q1 | `quarantine` action reachability contradiction. Registry spec §10 guarantees quarantined components are in `quarantinedComponents` and are excluded before selector fan-out — they never appear in `componentsById`. Orchestration spec §8 step 1 (defensive path) had produced `action: quarantine` for components that somehow appeared in the selector despite being quarantined. Simultaneously, §4 had included `quarantine` in the `SelectionDecision` action enum. This made Step 1 unreachable dead code while the enum still had to accommodate it. Open question 9-Q1 flagged this. | **Resolved Pass 4.7A (Option A):** `quarantine` is registry-phase state only in MVP — it is not a valid `SelectionDecision.action`. `quarantine` removed from the `SelectionDecision` action enum (§4 field table, action values table). Former `path: quarantine` replaced with `path: quarantine_boundary_violation`. Step 1 of the ladder rewritten as **boundary-violation detection**: if a quarantined component ID appears in the selector fan-out candidate set despite the registry guarantee, the orchestrator produces `action: include`, `path: quarantine_boundary_violation`, `confidence: low` and emits an `unexpected_quarantine_reference` planning error (not a warning). The component is not silently omitted — fail-open include is the safety net. Quarantine events under correct MVP operation are recorded only in `registryPhase` of `trace.json`. Quarantined components are not counted as budget savings, not in `decidedOmit`, not in any selector statistic. Unknown ID references (`reference_unknown`) remain a distinct event class — quarantined components are validated-but-excluded; unknown IDs were never seen by the registry. `quarantine` may be revisited for future non-MVP streaming-registry scenarios. 9-Q1 resolved/reference. |
| F-18 | ~~**major**~~ ✅ **resolved** | `06` §17.6 vs §18-Q4 | Global injection warning deduplication mechanism undefined. Orchestration spec §17.6 requires one global warning per planning run when `injectionSuspect: true` (`injection_suspect_warn_and_continue` or `injection_suspect_fail_open_all`). With 8 selectors running in fan-out, each selector (or the gate after each selector) could independently emit this warning. Deduplication responsibility — which module suppresses duplicates — is not specified. Open question 18-Q4 flags this. | Without a defined deduplication mechanism, implementations will emit 8 duplicate global warnings per run, confusing operator traces. | **Resolved Pass 4.7C (Option B):** The **orchestrator** is the sole owner of global injection warning emission. Selectors and per-decision injection gates must not emit the global warning codes independently. The orchestrator emits exactly one global warning per planning run after policy normalization, determined by the effective policy (`injection_suspect_warn_and_continue` for `warn_and_continue`; `injection_suspect_fail_open_all` for `fail_open_all`). Orchestrator maintains conceptual `globalInjectionWarningSent` boolean. Fallback cases (`halt_planning`, unknown/typo) emit one policy-level warning plus exactly one effective-policy global warning. Per-decision codes (`injection_suspect_omit_allowed`, `injection_suspect_policy_override`) are distinct and unaffected. Three new harness checks added: (1) global warning emitted more than once is a deduplication failure; (2) global warning in per-decision `warningsEmitted` is a trust-boundary violation; (3) global warning absent when `injectionSuspect: true` is a traceability failure. Canonical wording in Orchestration spec §17.6. 18-Q4 resolved/reference. |
| F-19 | ~~**major**~~ ✅ **resolved** | `06` §27; `04` §7.7 | `budgetHintSummary` divergence enforcement gap. **Resolved Pass 4.5B / ordering fixed Pass 4.5B.1:** Ownership assigned to the **Prompt Plan Generator**: PPG computes `budgetHintSummary` during final prompt-plan output assembly, after the Budgeter has produced its `BudgetReport`. PPG derives it strictly from `ResolvedSelectionDecision` records. PPG must never invent or change hint values. `budgetHintSummary` is optional in MVP; if absent, nothing special happens because the Budgeter has already completed its work directly from `resolvedSelectionDecisions`. **Budgeter does not consume `budgetHintSummary` in MVP.** Divergence between summary and resolved decisions is an Evaluation Harness failure — harness must report which field diverged. Three new harness checks added to Orchestration spec §27.8: (1) summary disagreement with resolved records, (2) summary produced by wrong module, (3) summary produced before conflict resolution is complete. `resolvedSelectionDecisions` remain the only canonical Budgeter input for budget hints. | Resolved — no further action needed. |
| F-20 | ~~**minor**~~ ✅ **resolved** | `06` §11 vs §17.4 | Injection gate ordering and conflict character loss. **Resolved/reference Pass 4.8C.** Decision: `SelectionDecision` shape is unchanged. `selectorTrace` is the canonical source for pre-gate `originalCandidateAction` / `originalCandidatePath` (via `actionChanged: true`). The Conflict Resolver consults these fields via `SelectionDecision.traceRefs` and may optionally summarize gate-conversion context in `conflictResolutionTrace` via 4 new optional fields: `hadGateConvertedDecisions`, `gateConvertedTraceRefs`, `preGateActions`, `preGatePaths`. Resolved action remains based on post-gate decisions; pre-gate omit is not restored. Two new harness checks added (§11.7). Canonical notes in Orchestration spec §17.4 (invariant), §17.6 (selectorTrace canonical note), §11.6 (optional trace fields). | Resolved — no action needed. |
| F-21 | ~~**minor**~~ ✅ **resolved** | `04` §7.3 vs `06` §17.1 | Injection-suspect event emitter ambiguity. Architecture §7.3 said selectors could independently trace injection-suspect events. Orchestration spec §17.1 establishes a strict boundary: Request Router detects, selectors consume a pre-computed boolean, only the post-ladder injection gate emits injection-related trace entries. | Implementors following the old Architecture wording would produce duplicate or misattributed trace entries. | **Resolved Pass 4.4:** Architecture §7.3 injection mitigation wording replaced with the correct Orchestration §17 boundary: detection is the Request Router's responsibility; selectors consume `requestSignals.injectionSuspect` boolean only; the post-ladder injection gate emits trace entries and applies overrides. |
| F-22 | ~~**minor**~~ ✅ **resolved** | `06` §12, 12-Q4 | History-malformed conflict case behavior undefined. **Resolved/reference Pass 4.8D (12-Q4).** Decision: Case 12 (`resolutionRule: history_malformed_fail_open`) added to Orchestration spec §11.5. History-malformed fail-open include wins over omit when no Priority 0–4 rule applies. Hard-protection preemption rule defined. `history_malformed_conflict` per-conflict warning + `history_malformed_conflict_occurred` global per-run warning (deduplicated). `losingDecisions` contains only true losers. 5 new harness checks in §11.7. Case 5 `losingDecisions` semantics also corrected (Pass 4.8D.1). | Resolved — no action needed. |

### 5.3 MVP / Non-MVP Boundary Findings

| Finding ID | Severity | Files / Sections | Description | Why it matters | Recommended next action |
|---|---|---|---|---|---|
| F-23 | ~~**major**~~ ✅ **already resolved** | `04` §7.3 vs `06` §7, §8 | `summarize` treated as active MVP selector action in Architecture. Orchestration MVP selector actions are `include`, `omit`, `defer`, and `reference_unknown`; `quarantine` is registry-phase state only after Pass 4.7A (F-17 resolved); `summarize` remains future-only. | MVP code might include a `summarize` code path that is never triggered. | **Already resolved prior to Pass 4.4:** Architecture §7.3 (Pass 4.2A) already explicitly states that `summarize` is **future-only** and not part of the MVP deterministic action set. Architecture §3 design goal #4 also confirms this. No further edit needed. |
| F-24 | ~~**major**~~ ✅ **resolved** | `06` §2.9 vs §18-Q2 | `halt_planning` injection policy value undefined in MVP. Orchestration spec §18-Q2 notes a possible third `injectionSuspectAction` value (`halt_planning`) for high-security deployments, explicitly excluded from MVP. However, `selectorPolicy` (§2.9) only defines `warn_and_continue` and `fail_open_all` as allowed values. No explicit validation rule prohibits an operator from setting `injectionSuspectAction: halt_planning` in an MVP config file. The fallback behavior for an unrecognized value is `warn_and_continue` (§17.3), but `halt_planning` is a semantically known value that should halt, not warn. | An operator testing a future value in MVP receives a silent, incorrect behavior (`warn_and_continue`) rather than a clear error. | **Resolved Pass 4.7B (Option B):** `halt_planning` is explicitly recognized as a known future/non-MVP reserved value in `selectorPolicy.injectionSuspectAction`. If an operator config supplies `halt_planning` in MVP: (1) the orchestrator recognizes it as a known-but-not-implemented value, not a typo; (2) emits `policy_value_not_implemented` planning warning; (3) applies `warn_and_continue` as the safe fallback; (4) records the fallback in the trace. This is distinct from `injection_action_unknown` (used for genuinely unrecognized/typo values). Halt planning semantics are not defined in MVP and must not be implemented. An `injectionSuspectAction` allowed-values table is now in Orchestration spec §2.9. 18-Q2 resolved/reference. |
| F-25 | ~~**major**~~ ✅ **resolved** | `06` §2.1 vs §5-Q1 | `requestSignals.injectionSuspect` boolean vs richer pattern-code struct: MVP boundary not locked. Orchestration spec §2.1 defines `injectionSuspect` as a boolean for MVP. Open question 5-Q1 asks whether a richer struct (list of matched pattern codes) is needed for audit purposes. The question is unresolved. If a future upgrade adds pattern codes without versioning the input contract, existing deterministic selectors receive an unexpected field shape with no migration path or backward-compatibility note. | Breaking change risk if the input contract is extended without a versioning mechanism. The MVP shape cannot be safely extended to a struct without an explicit versioning decision. | **Resolved Pass 4.7B (Option A):** `requestSignals.injectionSuspect` is frozen as `boolean` for MVP — this is the stable MVP contract. Malformed `injectionSuspect` (non-boolean when `requestSignals` is otherwise valid) → treat as `false`, emit `injection_suspect_malformed` planning warning. Future richer metadata (e.g., `injectionEvidenceCodes?: string[]`) must use optional additive fields only — must not replace or retype the boolean. A struct replacement of the boolean type requires an explicit future cross-spec pass with a versioning decision. Selectors must consume `injectionSuspect` as a boolean and must not inspect any richer-metadata field — that metadata belongs to the Request Router's output contract, not the selector input contract. Canonical wording and field-evolution note in Orchestration spec §2.1. 5-Q1 resolved/reference. |
| ~~F-26~~ | ~~**minor**~~ ✅ **resolved** | `04` §7.10 vs `06` §2.5, §5-Q3 | `runtimeCapabilities` has no version stamp in MVP. **Resolved/reference Pass 4.8E-2A (5-Q3 / F-26 — safe-defer).** Decision: non-MVP boundary note added to docs/06 §2.5 — `capabilityTimestamp` and `capabilityVersion` are future optional fields for adapter-supplied capability snapshots. In MVP, `runtimeCapabilities` is operator-authored per run; automated drift detection is out of scope; `runtimeLabel` is the only MVP runtime identifier. No warning emitted for absent version/timestamp fields in MVP. No MVP schema change required. | Resolved — no action needed. |
| F-27 | ~~**minor**~~ ✅ **resolved** | `06` §3.6; §5-Q6 | `selectorSummary` narrative string generation undefined for MVP. **Resolved Pass 4.5B.** The MVP template is now defined in Orchestration spec §3.6. Fixed deterministic template: `"{totalEvaluated} components evaluated. {decidedInclude} included, {decidedOmit} omitted, {decidedDefer} deferred ({defaultDefer} default, {runtimeUnavailableDefer} runtime-unavailable), {failOpenInclude} fail-open. {conflictsIdentified} conflict(s) identified."` Owner: Selector Orchestration output phase. Missing/null counts default to 0. Template must be deterministic. Model-generated narrative explicitly future-only — implementing it in MVP is a scope violation. 5-Q6 resolved/reference. | Resolved — no action needed unless future output_format fields are added. |
| ~~F-28~~ | ~~**minor**~~ ✅ **resolved** | `06` §4 (`path` table) vs §5-Q7 | `unavailable` action deferred; `defer / runtime_unavailable` plan-output representation. **Resolved/reference Pass 4.8E-2A (5-Q7 / F-28 — safe-defer).** Decision: MVP keeps `action: defer` + `path: runtime_unavailable`. A dedicated `action: unavailable` is future-only and must not be added to the action enum. `path` is required on every `deferredComponents[]` entry in `prompt-plan.json`; harness distinguishes defer subtypes by filtering `deferredComponents[]` on `path`. A future `deferSubtype` field may be considered only if harness or schema requirements prove it necessary. Notes added to docs/06 §4 path table `runtime_unavailable` row and key rules harness note; docs/04 §7.7 `deferredComponents[]` path-required clarification. | Resolved — no action needed. |
| F-29 | ~~**minor**~~ ✅ **resolved** | `06` §3.1 vs §5-Q8 | Scoped evaluation policy has no extension point defined. **Resolved/reference Pass 4.8D (5-Q8).** Decision: `candidateSetPolicy` is an internal orchestrator constant, MVP value `all_non_quarantined`. `candidateSetSummary` emitted in `registryPhase` before fan-out with `candidateSetPolicy`, `candidateSetSize`, `quarantinedExcluded`. Gap-check denominator is `candidateSetSize`. `conflictSummary.totalComponents` tied to `candidateSetSummary.candidateSetSize`. Unsupported policy halts with `unsupported_candidate_set_policy`. `reference_unknown` records tracked separately, not subtracted from denominator (4.8D.2). 5 harness checks. Future extension values (`by_type`, `by_prompt_family`, `explicit_component_ids`) named for reference only. See Orchestration spec §3.1. | Resolved — no action needed. |
| ~~F-30~~ | ~~**note**~~ ✅ **resolved** | `05` §13 vs `06` §23.4 | `budgetTrimmable: true` and `expensive_optional` interdependency undocumented. **Resolved/reference Pass 4.8E-2B (F-30 — safe-defer).** Decision: non-MVP cross-reference notes added to docs/06 §23.4 (in MVP, `expensive_optional` does not override `fail_open` not-trimmable rule; future `budgetTrimmable: true` + `expensive_optional` combined policy requires a dedicated future cross-spec design pass) and docs/05 §13 (reciprocal note: future interaction is deferred; `fail_open` components are never trimmed by MVP Budgeter regardless of `budgetHint`). | Resolved — no action needed. |
| ~~F-31~~ | ~~**note**~~ ✅ **resolved** | `06` §23.2 | Static `expensive_optional` threshold — implementation constant. **Resolved/reference Pass 4.8E-2B (F-31 — safe-defer).** Decision: implementation guide note added to docs/06 §23.2 — define a named constant `EXPENSIVE_OPTIONAL_THRESHOLD_DEFAULT = 500` rather than scattering the literal `500` inline; `thresholdUsed: 500` trace field (§23.5) makes the applied threshold harness-observable; no `defaultExpensiveOptionalThreshold` `selectorPolicy` input field in MVP — threshold configurability remains deferred. | Resolved — no action needed. |

> **No finding:** Model-assisted selectors are consistently marked future-only in both Architecture §7.3 and Orchestration spec §7 (MVP deterministic only). No boundary ambiguity.

---

## 6. Implementation-Readiness Gates

| Gate | Status | Reason | Required next action |
|---|---|---|---|
| **Gate A — Ready for JSON Schema?** | 🟢 **ready** | All major findings resolved. All schema-blocking and harness-blocking questions resolved (Passes 4.8B–4.8C). CLI-plan questions (9-Q2, 9-Q3, 12-Q4, 5-Q8) resolved (Pass 4.8D). All safe-defer items resolved as non-MVP notes (Pass 4.8E). Pass 4.8F full audit re-run confirmed **0 active open questions and 0 active findings**. Schema file creation is unblocked for all audited shapes. | No further blockers. Schema file creation may begin. |
| **Gate B — Ready for Evaluation Harness Spec?** | 🟢 **ready** | All three named Gate B blockers resolved in Pass 4.8C: 18-Q1 (`output_format` bias rule defined), 18-Q3 (familyConfidence escalation rule defined, `policyFallbackReasons[]` canonical), F-20 (pre-gate conflict character preserved via selectorTrace + optional conflict trace fields). 17 injection harness checks defined in Orchestration spec §17.7; 16 conflict harness checks in §11.7. The full evaluation harness spec may now be written for deterministic and injection gate test cases. | No further blockers for harness spec. |
| **Gate C — Ready for CLI MVP Implementation?** | 🟢 **ready** | All 14 major findings resolved. All CLI-plan-blocking questions resolved (Pass 4.8D). All 6 safe-defer open questions and 4 minor/note findings resolved as explicit non-MVP notes (Pass 4.8E). Pass 4.8F full audit re-run confirmed **0 active open questions. 0 active minor findings. 0 active note findings.** Verdict upgraded to `READY_FOR_IMPLEMENTATION_PLAN`. | `docs/11_CLI_MVP_IMPLEMENTATION_PLAN.md` creation is now authorized. Actual code implementation remains prohibited until the CLI MVP Implementation Plan is written and approved. |
| **Gate D — Ready for OpenClaw Adapter / Source Mapping?** | 🔴 **blocked** | By workspace rules, OpenClaw is intentionally reference/read-only. No adapter, source mapping, live OpenClaw state access, or mutation is in scope for MVP. Additionally, core correctness (Gate C) must be proven via fixture tests before any adapter contacts a live runtime. This gate is not expected to pass until well after CLI MVP validation. | Do not open this gate. OpenClaw adapter work is post-MVP and requires explicit operator approval per workspace rules. |

---

## 7. Recommended Next Passes

### Pass 4.2 — Schema-Blocking Cross-Spec Decisions (historical note)

> **Note:** The original Pass 4.2 planned to resolve F-01, F-02, F-03, F-12, and F-13 together. F-01 and F-02 were resolved in Pass 4.2A. The remaining three decisions are split into Pass 4.2B (F-03) and Pass 4.2C (F-12/F-13) below.

---

### Pass 4.2B — Resolve F-03: HistoryTurn Retention Field Rename (✅ completed)

> **Resolved in Pass 4.2B.** Architecture §7.6 renamed `HistoryTurn.retainPolicy` to `turnRetentionPolicy` with enum `always | unless_budget_critical | prefer_drop`. A disambiguation note was added to prevent future confusion with the component-level `retainPolicy`. Component-level `retainPolicy` in Registry spec is unchanged.

**Must not do:** Resolve F-12 or F-13 in this pass. Must not begin schema file creation. Must not implement code.

---

### Pass 4.2C — Resolve F-12 and F-13: Budgeter Input and Plan Shape (✅ completed)

> **Resolved in Pass 4.2C.** Architecture §7.5 now specifies Budgeter input as canonical `ResolvedSelectionDecision` records (per Orchestration spec §11/§27); budget-related fields (`budgetHint`, `budgetWarningCodes`, `tokensApproxObserved`, etc.) documented. Architecture §7.7 prompt plan output now includes `deferredComponents[]` as a distinct list from `selectedComponents[]` and `omittedComponents[]`. Deferred components must not be counted as budget savings. No schema files were created.

**Must not do:** Resolve F-03 in this pass (already handled in 4.2B). Must not begin schema file creation. Must not implement code.

---

### Pass 4.3 — Registry / Orchestration Spec Patch for Field Alignment (historical note)

> **Note:** The original Pass 4.3 planned to resolve F-04, F-05, F-06, F-07, and F-08 together. F-05 and F-07 were resolved in Pass 4.3A. The remaining decisions are split into Pass 4.3B (F-04/F-06) and Pass 4.3C (F-08) below.

---

### Pass 4.3A — Resolve F-05 and F-07: formatTag + promptFamily Alignment (✅ completed)

> **Resolved in Pass 4.3A.** Registry spec §3 Optional MVP Fields now includes `formatTag` (string | null, default null). Registry spec §7 atoms table now uses canonical `promptFamily=<value>`; all examples updated; compatibility note added. No schema files created.

---

### Pass 4.3B — Resolve F-04 and F-06: Atom Governance and budgetCritical Ambiguity (✅ completed)

> **Resolved in Pass 4.3B.** Registry spec §7 now has an Atom Governance subsection (canonical ownership, trace-atom vs evidenceRequired-atom distinction, unknown-atom behavior, future-atom governance). Active MVP atom set: `promptFamily=<v>`, `riskLevel=<v>`, `explicitUserConstraint=false`. `budgetCritical=true` moved to Not Supported in MVP (contradicts Orchestration §20 budget-enforcement invariant). 15-Q1 resolved. No schema files created.

---

### Pass 4.3C — Resolve F-08: Confidence Type Decision (✅ completed)

> **Resolved in Pass 4.3C.** `SelectionDecision.confidence` remains `high | medium | low` (string enum) per Orchestration spec §4 — the canonical owner. Architecture §7.8 TraceEntry conceptual example updated: `confidence: 0.95` (float) replaced with `confidence: "high"` (string enum). A canonical note added: numeric confidence scores (e.g., `requestSignals.familyConfidence: float`) use distinct field names and are not the selector-decision `confidence` field. No new fields invented. Orchestration §4 unchanged. No schema files created.

---

### Pass 4.4 — Architecture Alignment Patch (✅ completed)

> **Resolved in Pass 4.4.** Architecture aligned with canonical Orchestration/Registry contracts:
> - **F-09 resolved:** Architecture §7.4 stale 8-level conflict priority table replaced with reference to Orchestration spec §11.4. Priority 0 (runtime correctness pre-check) now reflected.
> - **F-10 already resolved:** Architecture §7.3 (Pass 4.2A) already had `summarize` marked future-only and the correct action enum. Confirmed — no further edit needed.
> - **F-11 resolved:** Architecture §7.1 `evidenceRequired: null` semantics corrected to match Registry spec §7: `null` means no additional evidence expression required, not "no omission allowed".
> - **F-16 resolved:** Orchestrator Gap-Check step added to Architecture §6 data flow between Selectors and Conflict Resolver, with description of its behavior. Request Router annotation also updated to reflect injection-suspect signal emission.
> - **F-21 resolved:** Architecture §7.3 injection-suspect wording replaced with correct Orchestration §17 boundary: Request Router detects, selectors consume boolean, post-ladder injection gate emits trace entries.
> - **F-23 already resolved:** Architecture §7.3 (Pass 4.2A) already had `summarize` as future-only. Confirmed — no further edit needed.
> No Orchestration or Registry specs were edited. No code implemented. No schema files created.

---

### Pass 4.5A — Trace Structure Decision (✅ completed)

> **Resolved in Pass 4.5A.** MVP trace container structure decided (Option D: keyed phase object):
> - **5-Q2 resolved/reference:** `selectorTrace` is embedded in `trace.json.selectorPhase.selectorTrace`. No separate `selector-trace.json` in MVP. Future externalization reserved as non-MVP extension.
> - **F-15 resolved:** Architecture §7.8 updated — `trace.json` is no longer described as a flat array; it is a keyed phase object with `run`, `requestPhase`, `registryPhase`, `selectorPhase`, `conflictPhase`, `budgetPhase`, `planPhase`, and `warnings` as minimum MVP keys. Orchestration spec §3.2 updated — `selectorTrace` embedded under `trace.json.selectorPhase.selectorTrace`. No code implemented. No schema files created. No Registry spec edited.

---

### Pass 4.5B — selectorSummary Template + budgetHintSummary Ownership (✅ completed)

> **Resolved in Pass 4.5B.**
> - **F-19 resolved:** `budgetHintSummary` ownership assigned to Prompt Plan Generator. PPG computes `budgetHintSummary` during final prompt-plan output assembly, after the Budgeter has produced its `BudgetReport`. PPG derives it strictly from `ResolvedSelectionDecision` records. Optional in MVP; if absent, nothing special happens because Budgeter has already operated on `resolvedSelectionDecisions`. `budgetHintSummary` is a final-assembly convenience output only; Budgeter does not consume it in MVP. Divergence = Evaluation Harness failure. Three new harness checks added to Orchestration spec §27.8. No second source of truth introduced. `resolvedSelectionDecisions` remain the only canonical Budgeter input. Architecture §7.7 and Orchestration spec §27.6 updated. *(Ordering contradiction fixed in Pass 4.5B.1; stale Audit wording cleaned up in Pass 4.5B.2.)*
> - **F-27 resolved:** Deterministic MVP template defined in Orchestration spec §3.6: `"{totalEvaluated} components evaluated. {decidedInclude} included, {decidedOmit} omitted, {decidedDefer} deferred ({defaultDefer} default, {runtimeUnavailableDefer} runtime-unavailable), {failOpenInclude} fail-open. {conflictsIdentified} conflict(s) identified."` Owner: Selector Orchestration output phase. Missing/null counts → 0. Model-generated narrative future-only (scope violation in MVP). Architecture §7.8 updated.
> - **5-Q6 resolved/reference:** Active open questions 22 → 21.
> No code implemented. No schema files created. No Registry spec edited.

---

### Pass 4.6 — Budgeter + Prompt Plan Generator Contract Audit (✅ completed)

> **Resolved in Pass 4.6.** F-14 resolved. Architecture §7.5 expanded with the canonical Budgeter contract:
> - **Option chosen:** Expand Architecture §7.5 only (Option A). A dedicated `docs/10_BUDGETER_SPEC.md` was not needed — the additions are bounded and fit cleanly in §7.5.
> - **Canonical Budgeter input:** `ResolvedSelectionDecision[]` from Orchestration spec §11/§27. Budgeter does not consume raw `SelectionDecision[]` or `budgetHintSummary` in MVP.
> - **Canonical Budgeter output:** `BudgetReport` — conceptual fields defined: `totalSelectedTokensApprox`, `totalDroppedTokensApprox`, `droppedComponents[]`, `budgetTarget`, `budgetUtilization`, `budgetOverflow`, `riskFlags[]`, `conservativeEstimatesUsed[]`, `trimTrace[]`. Not a schema file.
> - **No mutation:** Budgeter reads resolved decisions as read-only. Does not alter `finalAction`, `finalPath`, `budgetHint`, or any resolved decision field.
> - **budgetHint interpretation table:** All 5 canonical MVP values defined (`protected`, `over_budget_protected`, `candidate_optional`, `expensive_optional`, `unknown_cost`) plus absent/null fallback. No new values invented.
> - **Budget infeasibility:** `budgetOverflow: true` + `budget_infeasible_protected_component` risk flag; no halt in MVP; aligns with §25 warn-only policy.
> - **PPG handoff:** PPG receives unmodified `ResolvedSelectionDecision[]` + `BudgetReport` + `HistoryPlan`. PPG surfaces overflow and risk flags. PPG must not reinterpret `budgetHint` or override Budgeter. `budgetHintSummary` computed only after PPG holds both resolved decisions and `BudgetReport`.
> - **Evaluation expectations:** Expanded for each hint value, mutation invariant, and silent-overflow check.
> No Orchestration spec edited. No Registry spec edited. No code implemented. No schema files created.

---

### Pass 4.7A — Quarantine Reachability: F-17 / 9-Q1 (✅ completed)

> **Resolved in Pass 4.7A.** F-17 resolved (Option A: quarantine is registry-phase state only in MVP, not a SelectionDecision action):
> - `quarantine` removed from `SelectionDecision.action` enum (§4 field table and action values table).
> - Former `path: quarantine` replaced with `path: quarantine_boundary_violation` in the path values table.
> - Step 1 of the deterministic ladder rewritten as **boundary-violation detection**: if a quarantined component ID appears in the selector fan-out candidate set despite the registry guarantee, the orchestrator produces `action: include`, `path: quarantine_boundary_violation`, `confidence: low` and emits `unexpected_quarantine_reference` planning error. Component is not silently omitted — fail-open include is the safety net.
> - §1 Purpose wording updated: quarantine explicitly described as registry-phase state, not selector action.
> - §3.6 `selectorSummary` Meaning updated: quarantine events are not counted in selector-phase statistics.
> - Step 1 notes added: quarantine events recorded only in `registryPhase` of `trace.json`; quarantined components not counted as budget savings; unknown-ID references (`reference_unknown`) remain a distinct event class.
> - 9-Q1 resolved/reference. Active open questions: 21→20. Active unresolved major findings: 4→3 (remaining: F-18, F-24, F-25).
> - No Registry spec edits required — Registry spec §10/§11 already correctly defines quarantine as registry-phase state.
> - No Architecture spec edits required — Architecture already had no quarantine wording in selector sections.
> - No code implemented. No schema files created. No OpenClaw touched. No provider/model calls.

---

### Pass 4.7B — Injection Policy Input Contracts: F-24 / F-25 (✅ completed)

> **Resolved in Pass 4.7B.**
> - **F-24 resolved (Option B):** `halt_planning` is explicitly recognized as a known future/non-MVP reserved value in `selectorPolicy.injectionSuspectAction`. Not implemented in MVP. If operator config supplies it in MVP: orchestrator recognizes it as known-but-not-implemented (not a typo), emits `policy_value_not_implemented` planning warning, applies `warn_and_continue` safe fallback, records fallback in trace. This is distinct from `injection_action_unknown` (typo/unknown values). A new `injectionSuspectAction` allowed-values table added to Orchestration spec §2.9. 18-Q2 resolved/reference.
> - **F-25 resolved (Option A):** `requestSignals.injectionSuspect` is frozen as `boolean` for MVP. Malformed (non-boolean) `injectionSuspect` → treat as `false`, emit `injection_suspect_malformed` planning warning. Future richer metadata (e.g., `injectionEvidenceCodes?: string[]`) must use optional additive fields only — must not replace the boolean type. A struct replacement requires an explicit future cross-spec pass with versioning decision. Selectors consume boolean only; Request Router is sole detection owner. Field-evolution note and malformed-input behavior added to Orchestration spec §2.1. 5-Q1 resolved/reference.
> - **Gate A:** Unblocked — schema planning for `selectorPolicy` and `requestSignals` may now begin (see Gate A wording).
> - Active unresolved major: 3→1 (remaining: F-18 only). Active open questions: 20→18 (5-Q1 and 18-Q2 resolved/reference).
> - No Architecture spec edits required — Architecture contained no stale wording for these findings.
> - No code implemented. No schema files created. No OpenClaw touched. No provider/model calls.

### Pass 4.7C — Injection Warning Deduplication: F-18 / 18-Q4 (✅ completed)

> **Resolved in Pass 4.7C (Option B).**
> - **F-18 resolved:** The **orchestrator** is the sole owner of global injection warning emission. Selectors and per-decision injection gates must not emit `injection_suspect_warn_and_continue` or `injection_suspect_fail_open_all` independently. The orchestrator emits exactly one global warning per planning run after policy normalization, determined by the effective policy. Conceptual `globalInjectionWarningSent` boolean prevents re-emission across multiple selector invocations.
> - **Fallback interaction:** `halt_planning` and unknown/typo values each produce one policy-level warning + one effective-policy global warning (two total, distinct purposes).
> - **Per-decision codes unaffected:** `injection_suspect_omit_allowed` and `injection_suspect_policy_override` are per-decision codes written to `SelectionDecision.warnings` and `traceEntry.warningsEmitted` — not subject to the global deduplication rule.
> - **Three new harness checks added** to Orchestration spec §17.7: (1) global warning emitted more than once; (2) global warning in per-decision `warningsEmitted`; (3) global warning absent when `injectionSuspect: true`.
> - **18-Q4 resolved/reference.** Active open questions: 18→17.
> - **Active unresolved major: 1→0** (all 14 major findings now resolved).
> - **Gate B:** Improved — F-18 removed from blockers; 2 remaining blockers (9-Q4, 18-Q1).
> - **Gate C:** Unblocked from major findings — now partial; open questions remain.
> - Orchestration spec §7 recommended passes updated. Verdict reassessed — see §9.
> - No Architecture spec edits required — Architecture contained no stale wording for F-18.
> - No code implemented. No schema files created. No OpenClaw touched. No provider/model calls.

---

### Pass 4.8A — Pre-Implementation Triage (✅ completed)

> **Completed in Pass 4.8A.** 17 active open questions and 7 minor/note findings triaged into schema/harness/CLI-plan/safe-defer buckets. Recommended pass sequence 4.8B–F defined. Gate A schema blockers enumerated (12-Q2, 12-Q1, 9-Q5, 9-Q4, 15-Q2). No findings resolved. See §10 for full triage tables.

---

### Pass 4.8B — Schema/Harness Blocking Open Questions (✅ completed)

> **Completed in Pass 4.8B.** Five schema/harness-blocking open questions resolved:
> - **12-Q2 resolved:** `resolutionRule` is a strict enum of coded values. Canonical values defined in Orchestration spec §11.3.1a. Free strings prohibited in MVP. Unrecognized values are harness failures. Future additions require a cross-spec pass.
> - **12-Q1 resolved:** No-conflict components (single unambiguous decision) recorded in `noConflictComponentIds` (lightweight `string[]`) only. Full `conflictResolutionTrace` entries only for actual conflicts. Accounting preserved via `conflictSummary.noConflict`. Harness checks `noConflictComponentIds.length + conflictResolutionTrace.length` equals total evaluated candidates.
> - **9-Q5 resolved:** `confidence: high` for all Path B omissions. Path B is fully deterministic once registry fields are valid. Confidence describes selector-rule certainty, not registry-author judgment. `medium` or `low` Path B omit is a planning error.
> - **9-Q4 resolved:** `path_a_null_evidence` per-decision warning code emitted in `SelectionDecision.warnings` whenever Path A authorizes omit with `evidenceRequired: null`. Enables harness to distinguish intentional from accidental null-evidence omissions. Not a global planning warning. Registry semantics unchanged.
> - **15-Q2 resolved:** Active ID validation owned by core boundary / orchestrator before selector fan-out. Unknown active IDs → `active_id_unknown` planning warning per ID + `unknownActiveIds[]` list. Selectors receive pre-validated lists. `active_id_unknown` is distinct from `reference_unknown`. Selectors do not re-validate.
> - **Active open questions: 17 → 12.**
> - **Gate A:** Schema file creation now unblocked from the 5 schema-shape questions. Remaining caution: injection-gate and CLI-plan schema elements await Pass 4.8C/D.
> - **Gate B:** Improved — 9-Q4 resolved; one remaining blocker (18-Q1).
> - No code implemented. No schema files created. No Architecture/Registry spec edited beyond alignment needs. No OpenClaw touched. No provider/model calls.

---

### Pass 4.8C — Injection Gate Remaining Open Questions (✅ completed)

> **Resolved in Pass 4.8C** (docs/06 Pass 4.8C through 4.8C.2; Audit synced in Pass 4.8C-3).
> - **18-Q1 resolved:** Scoped output_format bias under `warn_and_continue` — `riskLevel: critical` or `riskLevel: high` output_format components whose ladder produces `omit` are upgraded to `include / fail_open` + `injection_suspect_policy_override`; `riskLevel: low`/`medium` components are not biased. Canonical rule in Orchestration spec §17.3.1.
> - **18-Q3 resolved:** If `injectionSuspect: true` and `familyConfidence < failOpenThreshold` (strict less-than), effective policy escalates to `fail_open_all`; uses existing `failOpenThreshold` (no new field); `family_confidence_fail_open_escalation` warning emitted once; `policyFallbackReasons: string[]` replaces singular `policyFallbackReason`; full policy chain table (7 cases) in Orchestration spec §17.3.4.
> - **F-20 resolved:** `SelectionDecision` shape unchanged; `selectorTrace` is canonical source for pre-gate `originalCandidateAction`/`originalCandidatePath` when `actionChanged: true`; Conflict Resolver may optionally summarize via `hadGateConvertedDecisions`, `gateConvertedTraceRefs`, `preGateActions`, `preGatePaths` in `conflictResolutionTrace`; two new harness checks added (§11.7). Canonical notes in Orchestration spec §17.4, §17.6, §11.6.
> - Gate B unblocked — 🟢 ready. 17 injection harness checks; 11 conflict harness checks defined.

---

### Pass 4.8D — CLI Plan Pre-Freeze Decisions (✅ completed)

> **Completed in Pass 4.8D** (docs/06 Pass 4.8D source sync + 4.8D.1 cleanup + 4.8D.2 accounting cleanup; Audit synced in Pass 4.8D-3).
> - **9-Q2 resolved:** Step 3 always emits `path: safety_override` for all four hard-protection conditions. Former `required_match` exception removed. Secondary `requiredWhen` match preserved as trace atoms in `evidence[]`/`constraintsApplied[]` using existing syntax — no new fields. `path: required_match` from Step 3 is a harness failure. Canonical wording in Orchestration spec §8 Step 3.
> - **9-Q3 resolved:** Singular canonical `type` per component. Primary selector for that `type` is sole evaluator. Multi-role intent resolved at registry authoring time by type choice or splitting into separate entries. No inter-selector handoff in MVP. Type-ownership violation if multiple primary selectors independently evaluate same component. Canonical wording in Orchestration spec §7 fan-out rule.
> - **12-Q4 / F-22 resolved:** Case 12 (`resolutionRule: history_malformed_fail_open`) added. History-malformed fail-open include wins over omit when no Priority 0–4 rule applies. Hard-protection preemption rule defined. `history_malformed_conflict` per-conflict warning; `history_malformed_conflict_occurred` global per-run warning (deduplicated). `losingDecisions` contains only true losers. 5 new harness checks (§11.7). Canonical wording in Orchestration spec §11.5 Case 12 and §11.3.1a.
> - **5-Q8 / F-29 resolved:** `candidateSetPolicy` is an internal orchestrator constant, MVP value `all_non_quarantined`. `candidateSetSummary` emitted in `registryPhase` before fan-out (`candidateSetPolicy`, `candidateSetSize`, `quarantinedExcluded`). Gap-check denominator is `candidateSetSize`. `conflictSummary.totalComponents` tied to `candidateSetSummary.candidateSetSize`. Unsupported policy halts with `unsupported_candidate_set_policy`. `reference_unknown` records tracked separately, not subtracted from denominator (4.8D.2). 5 candidateSetSummary harness checks. Future extension values named for reference only. Canonical wording in Orchestration spec §3.1.
> - **4.8D.1 cleanup:** Case 5 `losingDecisions` semantics fixed — only true losers; `multiple_include_merged` cannot hide hard protection; hard-protection harness row strengthened.
> - **4.8D.2 accounting cleanup:** §11.3.2 denominator tied to `candidateSetSummary.candidateSetSize`; §11.3.4 `conflictSummary.totalComponents` tied to `candidateSetSummary.candidateSetSize`; §13 DoD case count corrected 11→12.
> - Active open questions: 10→6. Active minor findings: 4→2. Gate C improved — all CLI-plan blockers resolved; remaining 6 open questions are safe-defer / Pass 4.8E scope.
> - No code implemented. No schema files created. No Architecture or Registry spec edited. No OpenClaw touched. No provider/model calls.

---

### Pass 4.8D-cache — Cache-Aware Prompt Layout Hook (✅ completed / non-blocking architecture hook)

> **Completed in Pass 4.8D-cache.** Cache-aware prompt layout recorded as an advisory, provider-agnostic PPG concern.
> - **Design decision recorded:** Cache-awareness is part of the Context Control Plane as an advisory prompt-layout optimization layer only. It is not a selector rule, not a schema requirement, not a safety gate, and not a CLI MVP implementation item.
> - **Boundaries confirmed:** Provider-agnostic. Advisory only — affects prompt ordering/layout, not selection safety. Must never authorize omission. Must never alter membership of `selectedComponents[]`, `omittedComponents[]`, or `deferredComponents[]`. Must never override safety, retention, hard-protection, user constraints, budget enforcement, or fail-open rules. Cache hints must not hide stale-content risk. Cache optimization is subordinate to safety, traceability, and correctness. Provider-specific cache implementation is future adapter work.
> - **Files updated:** `PROJECT_MASTER_PLAN.md` §14.0 (commercial wedge extended — cost-aware/cache-aware governance positioning added); `docs/04_PORTABLE_CORE_ARCHITECTURE.md` §7.7 (new subsection: Cache-Aware Component Ordering — stablePrefix/sessionPrefix/volatileSuffix buckets; stable/session/volatile classification; ordering invariants; future advisory hint fields listed as non-mandatory).
> - **docs/06 not touched.** Cache-aware layout is a downstream PPG concern; docs/06 owns selector-level contracts which are upstream of PPG ordering.
> - Not a finding. Not a blocker. No open-question count change. No gate status change. No verdict change.
> - No code implemented. No schema files created. No provider-specific cache language introduced. No new schema fields made mandatory. No new finding ID or open question added. No OpenClaw touched. No provider/model calls.

---

### Pass 4.8E — Safe-Defer Dispositions (✅ completed)

> **Completed in Pass 4.8E** (docs/06 Pass 4.8E-2A + 4.8E-2B safe-defer notes; docs/05 Pass 4.8E-2B F-30 cross-reference; Audit synced in Pass 4.8E-3B).
> - **5-Q3 / F-26 resolved:** Non-MVP boundary note added to docs/06 §2.5 — `capabilityTimestamp`/`capabilityVersion` future optional fields; no warning for absent version/timestamp in MVP; `runtimeLabel` only MVP runtime identifier.
> - **5-Q4 resolved:** Non-MVP note added to docs/06 §4 — `componentId` dual-use intentional in MVP; `unknownId` deferred to schema v1.1; harness must not compare `reference_unknown.componentId` against `componentsById`.
> - **5-Q5 resolved:** Non-MVP boundary note added to docs/06 §2.8 — MVP `userConstraints` operator-supplied high-trust only; session-derived constraints deferred to adapter spec.
> - **5-Q7 / F-28 resolved:** Notes added to docs/06 §4 and docs/04 §7.7 — MVP keeps `action: defer` + `path: runtime_unavailable`; `path` required on every `deferredComponents[]` entry; dedicated `action: unavailable` future-only.
> - **12-Q5 resolved:** Non-MVP boundary note added to docs/06 §11.3.4 — no `byPriority` field in MVP; per-priority counts derivable from `conflictResolutionTrace.resolutionRule`; harness must not parse `narrative`.
> - **15-Q5 resolved:** Evaluation Harness boundary note added to docs/06 §14.8 — runtime_capability omission fixtures must set `capabilityInventoryComplete: true`; with `false`/absent inventory, fail-open inclusion is correct MVP behavior.
> - **F-30 resolved:** Non-MVP cross-reference notes added to docs/06 §23.4 and docs/05 §13 — `expensive_optional` does not override `fail_open` not-trimmable in MVP; future `budgetTrimmable: true` + `expensive_optional` combined policy requires a dedicated future cross-spec design pass.
> - **F-31 resolved:** Implementation guide note added to docs/06 §23.2 — define `EXPENSIVE_OPTIONAL_THRESHOLD_DEFAULT = 500` as a named constant; `thresholdUsed` trace field makes threshold harness-observable; no `selectorPolicy` configurability in MVP.
> - Active open questions: 6→0. Active minor findings: 2→0. Active note findings: 2→0. Gate C: ready for Pass 4.8F final readiness reassessment.
> - No code implemented. No schema files created. No OpenClaw touched. No provider/model calls.

---

### Pass 4.8F — Final Readiness Reassessment (✅ completed)

> **Completed in Pass 4.8F.** Full audit re-run on disk. All source files read fresh (docs/09, docs/06, docs/04, docs/05, PROJECT_MASTER_PLAN.md). 0 active items confirmed across all categories. Gate A/C upgraded 🟡→🟢. Verdict upgraded to `READY_FOR_IMPLEMENTATION_PLAN`. `docs/11_CLI_MVP_IMPLEMENTATION_PLAN.md` creation now authorized. No code/schema/runtime/OpenClaw/provider touched.

> **Note:** Pass 4.8 was previously numbered Pass 4.7. Renumbered after Pass 4.6 was completed and a new Pass 4.7 inserted.

---

## 8. Explicit Non-Goals of This Audit

- ❌ No OpenClaw surgery, live state access, or mutation.
- ❌ No live runtime changes of any kind.
- ❌ No model-assisted selectors (future-only; not designed or implemented here).
- ❌ No provider or model calls.
- ❌ No prompt assembly implementation.
- ❌ No budget enforcement implementation.
- ❌ No JSON Schema file generation in this audit.
- ❌ No CLI MVP implementation in this audit.
- ❌ No source spec patching inside this audit document; source-spec patching happens only in explicitly scoped passes.
- ❌ No OpenClaw adapter design or source mapping.

**Product-direction reminder** (see `PROJECT_MASTER_PLAN.md` §14.0 and `docs/00_NORTH_STAR.md`): This project is a portable **Context Governance / Context Control Plane** for AI agent runtimes — not an agent runtime itself. OpenClaw is a reference system and possible future adapter, not a dependency. Adapters are post-MVP. MVP is CLI-only: input files in, `prompt-plan.json` / `trace.json` / `summary.md` out. No audit finding, gate status, or verdict is changed by this note.

---

## 9. Final Verdict

> ### ✅ READY_FOR_IMPLEMENTATION_PLAN
>
> **Verdict upgraded in Pass 4.8F.** `docs/11_CLI_MVP_IMPLEMENTATION_PLAN.md` creation is now authorized. Actual code implementation remains prohibited until the CLI MVP Implementation Plan is written and approved by the user.

### Evidence

| Category | Count | Implementation impact |
|---|---|---|
| Blocker findings | 0 remaining — F-01/F-02 resolved Pass 4.2A; F-03 resolved Pass 4.2B; F-12/F-13 resolved Pass 4.2C | No schema-blocking inconsistencies remain. |
| Major findings | 14 total; **0 active unresolved after Pass 4.7C** | All major findings resolved. |
| Minor findings | **0 active** (F-26/F-28 resolved Pass 4.8E; F-22 resolved Pass 4.8D; F-29 resolved Pass 4.8D; F-20 resolved Pass 4.8C; F-27 resolved Pass 4.5B) | All minor findings resolved. |
| Note findings | **0 active** (F-30/F-31 resolved Pass 4.8E) | All note findings resolved. |
| Active open questions (unresolved) | **0** (all 27 inventory rows are resolved/reference after Pass 4.8E) | No open questions remain. |

### Why `READY_FOR_IMPLEMENTATION_PLAN` (Pass 4.8F assessment)

- **0 blocker findings remain.** All 5 blocker findings resolved: F-01/F-02 in Pass 4.2A, F-03 in Pass 4.2B, F-12/F-13 in Pass 4.2C.
- **0 active unresolved major findings** (14 total; all resolved through Pass 4.7C).
- **0 active open questions** (all 27 rows resolved/reference). **0 active minor findings** (F-26/F-28 resolved Pass 4.8E). **0 active note findings** (F-30/F-31 resolved Pass 4.8E). All safe-defer items closed as explicit non-MVP notes in Pass 4.8E.
- **Gate A (JSON Schema) — 🟢 ready.** Schema file creation unblocked for all audited shapes (Pass 4.8F: upgraded from 🟡).
- **Gate B (Evaluation Harness Spec) — 🟢 ready.** All three named blockers resolved in Pass 4.8C. 17 injection harness checks + 16 conflict harness checks defined.
- **Gate C (CLI MVP Implementation Plan) — 🟢 ready.** Pass 4.8F full audit re-run confirmed 0 active items across all categories (upgraded from 🟡).
- **Remaining risks (non-blocking):** (1) Schema-authoring must strictly adhere to finalized trace and decision shapes in docs/06. (2) Harness-authoring must assert on the 17 injection-gate and 16 conflict-resolution harness checks. (3) OpenClaw/n8n/Telegram adapters must remain post-MVP and read-only per workspace rules. (4) Cache-aware prompt ordering is advisory/PPG-only — must never authorize omission or alter component membership. (5) Future non-MVP fields (e.g., `unknownId`, `byPriority`, `capabilityTimestamp`) must not be added in MVP without an explicit cross-spec pass.

### Completed prerequisite checklist (historical record)

1. Pass 4.2A/B/C: F-01/F-02/F-03/F-12/F-13 resolved.
2. Pass 4.3A/B/C: F-05/F-07/F-04/F-06/F-08 resolved.
3. Pass 4.4: F-09/F-11/F-16/F-21 resolved.
4. Pass 4.5A: F-15 resolved.
5. Pass 4.5B: F-19/F-27 resolved. (Pass 4.5B.1: F-19 ordering contradiction fixed — PPG computes during final assembly, Budgeter does not consume `budgetHintSummary` in MVP.)
6. **Pass 4.6:** Budgeter + Prompt Plan Generator contract audit (F-14). ✅ Completed.
7. **Pass 4.7A:** F-17 (quarantine reachability). ✅ Completed.
8. **Pass 4.7B:** Resolve F-24 (`halt_planning` selectorPolicy) and F-25 (`injectionSuspect` boolean/struct boundary). ✅ Completed.
9. **Pass 4.7C:** Resolve F-18 (global injection warning deduplication / 18-Q4). ✅ Completed.
10. **Pass 4.8A:** Pre-implementation triage — classify remaining open questions and minor/note findings into sequencing buckets. ✅ Completed.
11. **Pass 4.8B:** Resolve schema-blocking open questions (12-Q2, 12-Q1, 9-Q5, 9-Q4, 15-Q2). ✅ Completed.
12. **Pass 4.8C:** Resolve injection gate remaining questions (18-Q1, 18-Q3, F-20). ✅ Completed. Gate B 🟢 ready.
13. **Pass 4.8D:** Resolve CLI-plan-blocking questions (9-Q2, 9-Q3, 12-Q4/F-22, 5-Q8/F-29). ✅ Completed. Active open questions 10→6; active minor findings 4→2.
13a. **Pass 4.8D-cache:** Record cache-aware prompt layout as advisory/provider-agnostic PPG concern. ✅ Completed. No counts/gates/verdict changed. docs/06 untouched.
14. **Pass 4.8E:** Dispose of safe-defer items (5-Q3/F-26, 5-Q4, 5-Q5, 5-Q7/F-28, 12-Q5, 15-Q5, F-30, F-31). ✅ Completed.
15. **Pass 4.8F:** Final readiness reassessment — verdict upgrade, CLI implementation plan gate. ✅ Completed.

---

## 10. Pre-Implementation Triage (Pass 4.8A)

> [!NOTE]
> This section was originally added in Pass 4.8A for pre-implementation triage. Rows now record final dispositions through Pass 4.8E. Bucket definitions are preserved for historical traceability. No new decisions are being made in this cleanup pass.
> - **Schema-before-schema** — must resolve before any schema file is created
> - **Harness-before-harness** — must resolve before the full Evaluation Harness spec is written
> - **CLI-plan-before-CLI** — must resolve before the CLI MVP Implementation Plan is approved
> - **Implementation-before-code** — must resolve before any implementation code is written
> - **Safe-defer** — can be documented as an explicit non-MVP note; no immediate block

### 10.1 Open Question Triage

| ID | Short title | Bucket | Disposition | Rationale | Suggested next pass |
|---|---|---|---|---|---|
| ~~12-Q2~~ | ~~`resolutionRule` enum vs string~~ | ~~Schema-before-schema~~ | **Resolved Pass 4.8B** — strict enum; canonical values in Orchestration spec §11.3.1a | Enum prevents typos; harness can reliably check enum values | ✅ Pass 4.8B |
| ~~12-Q1~~ | ~~No-op conflict trace granularity~~ | ~~Schema-before-schema~~ | **Resolved Pass 4.8B** — `noConflictComponentIds` lightweight ID list for no-conflict; full trace entries for actual conflicts only | Shape finalized; accounting preserved via `conflictSummary.noConflict` | ✅ Pass 4.8B |
| ~~9-Q5~~ | ~~Path B confidence convention~~ | ~~Schema-before-schema~~ | **Resolved Pass 4.8B** — `confidence: high` for all Path B decisions | Rule is fully deterministic; `medium`/`low` are planning errors | ✅ Pass 4.8B |
| ~~9-Q4~~ | ~~Path A null evidence warning code~~ | ~~Schema-before-schema~~ | **Resolved Pass 4.8B** — `path_a_null_evidence` per-decision warning code in `SelectionDecision.warnings` when `evidenceRequired: null` | Enables unambiguous harness checks; not a global planning warning | ✅ Pass 4.8B |
| ~~15-Q2~~ | ~~Active ID validation ownership~~ | ~~Schema-before-schema~~ | **Resolved Pass 4.8B** — core boundary/orchestrator validates before fan-out; unknown IDs → `active_id_unknown` + `unknownActiveIds[]`; selectors treat lists as trusted | Distinct from `reference_unknown`; no duplicate validation logic across 8 selectors | ✅ Pass 4.8B |
| ~~18-Q1~~ | ~~`output_format` injection bias~~ | ~~Harness-before-harness~~ | **Resolved Pass 4.8C** — scoped bias: `riskLevel: critical/high` output_format omit → `include/fail_open` + `injection_suspect_policy_override` under `warn_and_continue`; low/medium not biased | Gate B blocker; now resolved | ✅ Pass 4.8C |
| ~~18-Q3~~ | ~~`injectionSuspect` + low `familyConfidence`~~ | ~~Harness-before-harness~~ | **Resolved Pass 4.8C** — `injectionSuspect: true` + `familyConfidence < failOpenThreshold` → effective `fail_open_all`; `policyFallbackReasons[]`; `family_confidence_fail_open_escalation` warning; policy chain table in §17.3.4 | Gate B blocker; now resolved | ✅ Pass 4.8C |
| ~~9-Q2~~ | ~~Hard-include path for mandatory vs safety_critical~~ | ~~CLI-plan-before-CLI~~ | **Resolved Pass 4.8D** — Step 3 always `safety_override`; `required_match` exception removed; secondary `requiredWhen` match as trace atoms in `evidence[]`/`constraintsApplied[]` only; `path: required_match` from Step 3 is harness failure | Ladder rule finalized; trace unambiguous | ✅ Pass 4.8D |
| ~~9-Q3~~ | ~~Multi-role component type ownership~~ | ~~CLI-plan-before-CLI~~ | **Resolved Pass 4.8D** — singular `type`; primary selector sole evaluator; multi-role resolved at registry authoring time; no inter-selector handoff; type-ownership violation if multiple primary selectors evaluate same component | Selector orchestrator rule clear; single-type-per-component enforced | ✅ Pass 4.8D |
| ~~12-Q4~~ | ~~History malformed + conflict case~~ | ~~CLI-plan-before-CLI~~ | **Resolved Pass 4.8D** — Case 12 (`history_malformed_fail_open`); hard-protection preempts; `history_malformed_conflict` per-conflict; `history_malformed_conflict_occurred` global deduplicated; `losingDecisions` only true losers; 5 harness checks | Conflict Resolver implementation gap closed; Case 12 in §11.5 | ✅ Pass 4.8D |
| ~~5-Q8~~ | ~~Scoped evaluation policy~~ | ~~CLI-plan-before-CLI~~ | **Resolved Pass 4.8D** — `candidateSetPolicy` constant (MVP: `all_non_quarantined`); `candidateSetSummary` in `registryPhase`; gap-check denominator = `candidateSetSize`; `conflictSummary.totalComponents` tied to `candidateSetSize`; unsupported policy halts; `reference_unknown` not subtracted from denominator (4.8D.2); 5 harness checks | Gap-detection denominator explicit; extension point defined | ✅ Pass 4.8D |
| ~~12-Q5~~ | ~~`conflictSummary` breakdown by priority~~ | **Safe-defer** | **Resolved/reference Pass 4.8E-2B.** No `byPriority` field in MVP; derive per-priority counts from `conflictResolutionTrace.resolutionRule`; harness must not parse `narrative`. See docs/06 §11.3.4. | — | ✅ Pass 4.8E |
| ~~15-Q5~~ | ~~`capabilityInventoryComplete: false` omission gate~~ | **Safe-defer** | **Resolved/reference Pass 4.8E-2B.** runtime_capability omission fixtures must set `capabilityInventoryComplete: true`; false/missing inventory → fail-open inclusion is correct. See docs/06 §14.8. | — | ✅ Pass 4.8E |
| ~~5-Q3~~ | ~~`runtimeCapabilities` versioning~~ | **Safe-defer** | **Resolved/reference Pass 4.8E-2A.** `capabilityTimestamp`/`capabilityVersion` future optional; no warning for absent version/timestamp in MVP. See docs/06 §2.5. | tied to F-26 | ✅ Pass 4.8E |
| ~~5-Q4~~ | ~~`reference_unknown` field clarity~~ | **Safe-defer** | **Resolved/reference Pass 4.8E-2A.** `componentId` dual-use intentional in MVP; `unknownId` deferred to schema v1.1; harness must not compare against `componentsById`. See docs/06 §4. | — | ✅ Pass 4.8E |
| ~~5-Q5~~ | ~~`userConstraints` trust boundary~~ | **Safe-defer** | **Resolved/reference Pass 4.8E-2A.** MVP operator-supplied high-trust only; session-derived constraints deferred to adapter spec. See docs/06 §2.8. | — | ✅ Pass 4.8E |
| ~~5-Q7~~ | ~~Dedicated `unavailable` action~~ | **Safe-defer** | **Resolved/reference Pass 4.8E-2A.** `defer` + `path: runtime_unavailable` for MVP; `path` required on `deferredComponents[]`; harness filters on `path`; dedicated action deferred. See docs/06 §4; docs/04 §7.7. | tied to F-28 | ✅ Pass 4.8E |

---

### 10.2 Minor and Note Finding Triage

| Finding ID | Severity | Bucket | Blocks schema? | Blocks harness? | Blocks CLI plan? | Blocks code? | Disposition | Suggested next pass |
|---|---|---|---|---|---|---|---|---|
| ~~F-20~~ | minor | ~~Harness-before-harness~~ | no | yes | no | no | **Resolved Pass 4.8C** — `selectorTrace` canonical for pre-gate actions; 4 optional conflict trace fields; `SelectionDecision` shape unchanged; 2 new harness checks (§11.7) | ✅ Pass 4.8C |
| ~~F-22~~ | minor | ~~CLI-plan-before-CLI~~ | no | partial | yes | partial | **Resolved Pass 4.8D** — Case 12 (`history_malformed_fail_open`) in §11.5; hard-protection preemption; `history_malformed_conflict` + `history_malformed_conflict_occurred` warnings; `losingDecisions` only true losers; 5 harness checks (§11.7); Case 5 `losingDecisions` semantics fixed (4.8D.1) | ✅ Pass 4.8D |
| ~~F-26~~ | minor | **Safe-defer** | no | no | no | no | **Resolved/reference Pass 4.8E-2A.** Non-MVP boundary note added to docs/06 §2.5 — `capabilityTimestamp`/`capabilityVersion` future optional; no warning for absent version/timestamp; `runtimeLabel` only MVP identifier. | ✅ Pass 4.8E |
| ~~F-28~~ | minor | **Safe-defer** | no | no | no | no | **Resolved/reference Pass 4.8E-2A.** `defer` + `path: runtime_unavailable` for MVP; `path` required on `deferredComponents[]`; harness filters by `path`; dedicated `action: unavailable` future-only. Notes in docs/06 §4 + docs/04 §7.7. | ✅ Pass 4.8E |
| ~~F-29~~ | minor | ~~CLI-plan-before-CLI~~ | no | no | partial | partial | **Resolved Pass 4.8D** — `candidateSetPolicy` constant (MVP: `all_non_quarantined`); `candidateSetSummary` in `registryPhase`; gap-check denominator = `candidateSetSize`; `conflictSummary.totalComponents` tied to `candidateSetSize`; unsupported policy halts; `reference_unknown` not subtracted from denominator (4.8D.2); 5 harness checks | ✅ Pass 4.8D |
| ~~F-30~~ | note | **Safe-defer** | no | no | no | no | **Resolved/reference Pass 4.8E-2B.** Cross-reference notes added to docs/06 §23.4 and docs/05 §13 — `expensive_optional` does not override `fail_open` not-trimmable in MVP; future combined `budgetTrimmable: true` + `expensive_optional` policy requires future cross-spec design pass. | ✅ Pass 4.8E |
| ~~F-31~~ | note | **Safe-defer** | no | no | no | no | **Resolved/reference Pass 4.8E-2B.** Implementation guide note added to docs/06 §23.2 — define `EXPENSIVE_OPTIONAL_THRESHOLD_DEFAULT = 500`; `thresholdUsed` trace field makes threshold harness-observable; no `selectorPolicy` configurability in MVP. | ✅ Pass 4.8E |

---

### 10.3 Recommended Next-Pass Sequence

| Pass | Scope | Primary items | Gate impact | Status |
|---|---|---|---|---|
| **4.8B** | Schema/Harness Blocking | 12-Q2, 12-Q1, 9-Q5, 9-Q4, 15-Q2 | Gate A: schema file creation unblocked | ✅ Completed |
| **4.8C** | Injection Gate Remaining | 18-Q1, 18-Q3, F-20 | Gate B: 🟢 ready | ✅ Completed |
| **4.8D** | CLI Plan Pre-Freeze | 9-Q2, 9-Q3, 12-Q4/F-22, 5-Q8/F-29 | Gate C improved: CLI-plan blockers resolved; 6 safe-defer open questions remain | ✅ Completed |
| **4.8D-cache** | Cache-Aware Prompt Layout Hook | Advisory PPG ordering; stablePrefix/sessionPrefix/volatileSuffix buckets; future hint fields listed | Non-blocking; no gate/count/verdict change | ✅ Completed |
| **4.8E** | Safe-Defer Dispositions | 5-Q3/F-26, 5-Q4, 5-Q5, 5-Q7/F-28, 12-Q5, 15-Q5, F-30, F-31 | Open-question backlog cleared: 6→0; minor findings 2→0; note findings 2→0; Gate C ready for Pass 4.8F reassessment | ✅ Completed |
| **4.8F** | Final Readiness Reassessment | Full audit re-run on disk; 0 active items confirmed; Gate A/C upgraded 🟡→🟢; verdict upgraded NEEDS_CROSS_SPEC_CLEANUP_FIRST→READY_FOR_IMPLEMENTATION_PLAN | Verdict advanced; `docs/11_CLI_MVP_IMPLEMENTATION_PLAN.md` creation authorized | ✅ Completed |

> **Constraint:** Pass 4.8B must precede schema file creation. Pass 4.8C must precede full harness spec. Pass 4.8D must precede CLI implementation planning. Passes 4.8C and 4.8D may run in parallel if scope disciplines are maintained.

*Pass 4.8B: Five schema/harness-blocking open questions resolved (12-Q2, 12-Q1, 9-Q5, 9-Q4, 15-Q2). Gate A unblocked for schema files.*
*Pass 4.8B.1 fixes: stale no-conflict trace wording corrected in Orchestration §11.1/§11.6; Conflict Resolution DoD output and harness-check counts updated (§13); Audit §2 source-sync row updated to 4.8B + 4.8B.1; Audit §3 Conflict Resolution surface line updated to 9 harness checks with noConflictComponentIds / resolutionRule enum noted. 4.8B decisions unchanged; 12 active open questions, 0 active unresolved major, 5 active minor, 2 active note, Gates A/B/C partial, Gate D blocked, verdict NEEDS_CROSS_SPEC_CLEANUP_FIRST unchanged. No code implemented. No schema files created. No Architecture or Registry spec edited. No OpenClaw touched. No provider/model calls.*
*Pass 4.8B.2 fixes: Audit top version metadata aligned with 4.8B.1 cleanup; Orchestration §11.6 stale no-op/no_conflict examples removed from actual-conflict trace field table — resolutionRule example updated to actual-conflict values only; losingDecisions description updated to remove "Empty for no-op". 4.8B decisions unchanged; counts, gates, and verdict unchanged. No code implemented. No schema files created. No Architecture or Registry spec edited. No OpenClaw touched. No provider/model calls.*
*Pass 4.8C sync (Audit — Pass 4.8C-3): Audit synced with docs/06 Pass 4.8C + 4.8C.1 + 4.8C.2 (completed prior to this Audit pass). 18-Q1 resolved/reference (scoped output_format bias: riskLevel critical/high omit → include/fail_open under warn_and_continue; low/medium not biased). 18-Q3 resolved/reference (injectionSuspect + familyConfidence < failOpenThreshold → effective fail_open_all; policyFallbackReasons[] replaces singular policyFallbackReason; policy chain table in §17.3.4). F-20 resolved/reference (SelectionDecision shape unchanged; selectorTrace canonical for pre-gate originalCandidateAction/originalCandidatePath; 4 optional conflictResolutionTrace fields; 2 new harness checks in §11.7). Active open questions: 12→10. Active minor findings: 5→4. Active note findings: 2. Active unresolved major: 0. Gate B 🟢 ready. Gate A partial (CLI-plan schema elements still pending Pass 4.8D). Gate C partial (10 active open questions; 9-Q2/9-Q3/12-Q4/5-Q8 remain CLI-plan-before-CLI). Verdict unchanged: NEEDS_CROSS_SPEC_CLEANUP_FIRST. Next: Pass 4.8D — CLI Plan Pre-Freeze Decisions. docs/06 already completed through 4.8C.2 cleanup; docs/09 now synchronized. No code implemented. No schema files created. No Architecture or Registry spec edited. No OpenClaw touched. No provider/model calls.*
*Pass 4.8C-3.1 fixes: Audit structure cleaned up after 4.8C sync — duplicate/misplaced §7 Pass 4.8A/B/C/D/E/F blocks, §8, §9, and §10 removed (these were injected into the file body by the batch-3 edit); orphaned historical footer notes (Pass 4.5B through 4.7B) removed from inside §10.3; §10 triage tables restored to canonical compact form; §8 stale non-goal wording corrected ("No source spec patching in this audit (patching is deferred to Passes 4.2–4.4)" → "No source spec patching inside this audit document; source-spec patching happens only in explicitly scoped passes"). Counts, gates, decisions, and verdict unchanged. No code implemented. No schema files created. No source specs edited. No OpenClaw touched. No provider/model calls.*
*Pass 4.8D sync (Audit — Pass 4.8D-3): Audit synced with docs/06 Pass 4.8D source sync + 4.8D.1 cleanup + 4.8D.2 accounting cleanup. 9-Q2 resolved/reference (Step 3 always `path: safety_override`; `required_match` exception removed; secondary `requiredWhen` match as trace atoms in `evidence[]`/`constraintsApplied[]` only). 9-Q3 resolved/reference (singular canonical `type`; multi-role resolved at registry authoring time; no inter-selector handoff). 12-Q4 / F-22 resolved/reference (Case 12 `history_malformed_fail_open`; hard-protection preemption; `history_malformed_conflict` per-conflict + `history_malformed_conflict_occurred` global deduplicated; `losingDecisions` only true losers; 5 harness checks). 5-Q8 / F-29 resolved/reference (`candidateSetPolicy` constant MVP `all_non_quarantined`; `candidateSetSummary` in `registryPhase`; gap-check denominator = `candidateSetSize`; `conflictSummary.totalComponents` tied to `candidateSetSize`; unsupported policy halts; `reference_unknown` not subtracted from denominator; 5 harness checks). Active open questions: 10→6. Active minor findings: 4→2. Active note findings: 2. Active unresolved major: 0. Gate B 🟢 ready (unchanged). Gate A improved (CLI-plan schema elements now unblocked). Gate C improved (6 remaining open questions all safe-defer / Pass 4.8E scope). Verdict unchanged: NEEDS_CROSS_SPEC_CLEANUP_FIRST. Next: Pass 4.8E — Safe-Defer Dispositions. No code implemented. No schema files created. No Architecture or Registry spec edited. No OpenClaw touched. No provider/model calls.*
*Pass 4.8D-cache (non-blocking architecture hook): Cache-aware prompt layout hook recorded. Advisory, provider-agnostic PPG ordering principle added to docs/04 §7.7 (stablePrefix/sessionPrefix/volatileSuffix buckets; stable/session/volatile classification; ordering invariants; future advisory hint fields listed as non-mandatory). Commercial wedge extended in PROJECT_MASTER_PLAN.md §14.0 (cost-aware/cache-aware governance positioning). docs/06 not touched. No provider-specific cache language. No new schema fields made mandatory. No new finding ID or open question added. Active open questions: 6 (unchanged). Active minor findings: 2 (unchanged). Active note findings: 2 (unchanged). Active unresolved major: 0 (unchanged). Gates unchanged. Verdict unchanged: NEEDS_CROSS_SPEC_CLEANUP_FIRST. Next pass remains 4.8E — Safe-Defer Dispositions. No code implemented. No schema files created. No OpenClaw touched. No provider/model calls.*
*Pass 4.8E sync (Audit — Pass 4.8E-3B): Audit synced with docs/06 Pass 4.8E-2A + 4.8E-2B safe-defer notes and docs/05 Pass 4.8E-2B F-30 cross-reference. 5-Q3/F-26 resolved/reference (capabilityTimestamp/capabilityVersion future optional; no warning in MVP; docs/06 §2.5 note). 5-Q4 resolved/reference (componentId dual-use intentional; unknownId deferred to schema v1.1; docs/06 §4 note). 5-Q5 resolved/reference (MVP operator-supplied high-trust; session-derived deferred; docs/06 §2.8 note). 5-Q7/F-28 resolved/reference (defer + runtime_unavailable for MVP; path required on deferredComponents[]; docs/06 §4 + docs/04 §7.7 notes). 12-Q5 resolved/reference (no byPriority field; derive from conflictResolutionTrace.resolutionRule; harness must not parse narrative; docs/06 §11.3.4 note). 15-Q5 resolved/reference (runtime_capability omission fixtures must set capabilityInventoryComplete: true; fail-open inclusion correct when incomplete; docs/06 §14.8 note). F-30 resolved/reference (expensive_optional does not override fail_open not-trimmable; future budgetTrimmable + expensive_optional combined policy deferred; docs/06 §23.4 + docs/05 §13 notes). F-31 resolved/reference (EXPENSIVE_OPTIONAL_THRESHOLD_DEFAULT = 500 named constant; thresholdUsed trace field; no selectorPolicy configurability; docs/06 §23.2 note). Active open questions: 6→0. Active minor findings: 2→0. Active note findings: 2→0. Active unresolved major: 0 (unchanged). Gate B 🟢 ready (unchanged). Gate C: ready for final reassessment — 0 active open questions; safe-defer dispositions complete; pending Pass 4.8F verdict upgrade. Verdict unchanged: NEEDS_CROSS_SPEC_CLEANUP_FIRST. Next: Pass 4.8F — Final Readiness Reassessment. No code implemented. No schema files created. No OpenClaw touched. No provider/model calls.*
*Pass 4.8F (Audit — final readiness reassessment): Full audit re-run on disk. All source files read fresh (docs/09, docs/06, docs/04, docs/05, PROJECT_MASTER_PLAN.md). Confirmed: 0 active open questions; 0 active unresolved major findings; 0 active minor findings; 0 active note findings; 0 active blocker findings. All 27 open-question inventory rows resolved/reference; all 31 finding rows resolved. Gate A upgraded 🟡→🟢 (schema file creation authorized; no active blockers). Gate B confirmed 🟢 (unchanged; 17 injection harness checks + 16 conflict harness checks defined). Gate C upgraded 🟡→🟢 (CLI MVP Implementation Plan creation authorized). Gate D remains 🔴 blocked (post-MVP; OpenClaw adapter out of scope). Verdict upgraded: NEEDS_CROSS_SPEC_CLEANUP_FIRST → READY_FOR_IMPLEMENTATION_PLAN. docs/11_CLI_MVP_IMPLEMENTATION_PLAN.md creation is now authorized. Actual code implementation remains prohibited until the CLI MVP Implementation Plan is written and approved. Remaining non-blocking risks recorded in §9. No source spec edits (docs/06, docs/04, docs/05 not touched). No new findings invented. No code implemented. No schema files created. No OpenClaw touched. No provider/model calls.*

*Pass 4.9C-2C status sync: Documentation status sync after Batch A/B schema creation. Batch A shared schemas created in Pass 4.9C-2A (enums.shared, prompt-family, warning-code); Batch B input schemas created in Pass 4.9C-2B (active-ids, runtime-capabilities, history-state-summary, budget-state, user-constraints, selector-policy); Batch B audit completed in Pass 4.9C-2B.2 with no edits required. Stale “No schema files” status lines updated in docs/09 header, docs/11 header + §12 table, and docs/12 header + §1 scope table + §11 status table. All historical pass notes inside this document remain unchanged — each correctly states “No schema files created” for the pass in question, which predated schema creation. Runtime code, harness fixtures, and remaining schema batches (internal data objects, output files) are not yet started. docs/05, docs/06, docs/04, docs/12 canonical spec sections unchanged. No schema files edited. No source/test/fixture/runtime/OpenClaw/provider work.*

*Phase 13 status-sync note (2026-06-03): The historical Pass 4.8F verdict below and in §1/§9/§10.3 is retained as the pre-implementation audit record and must not be rewritten. At the time of Pass 4.8F, the verdict was `READY_FOR_IMPLEMENTATION_PLAN` and code implementation was correctly described as prohibited until the plan was approved. Current Phase 13 status: CLI MVP implementation plan (`docs/11`) was reviewed and explicitly approved by the user; all phases (0–12) were implemented and tested; 651/651 tests pass; Gate B is `SATISFIED WITH 1 APPROVED SKIP(S)` (27 passed, 1 approved-skipped — Fixture 13 `safety_hard_protection`, covered by unit test SHP-1); Gate D remains intentionally out of MVP scope. Future source code changes require separate explicit approval. No audit findings, open questions, gates, or canonical audit content were changed in this status-sync note.*
